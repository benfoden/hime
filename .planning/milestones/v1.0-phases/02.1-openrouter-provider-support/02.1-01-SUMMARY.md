---
phase: 02.1-openrouter-provider-support
plan: "01"
subsystem: api
tags: [openrouter, typescript, chrome-extension, translation-provider, unit-tests]

# Dependency graph
requires:
  - phase: 02-prompt-quality-error-hardening
    provides: TranslationProvider interface, classifyError, stripWrappers, buildSystemPrompt, provider abstraction pattern

provides:
  - OpenRouterProvider class implementing TranslationProvider (OpenAI-compatible format)
  - Extended ProviderConfig.provider union to include 'openrouter'
  - PROVIDER_MODELS.openrouter empty array (dynamic model fetch design)
  - Background service worker registration for openrouter provider
  - manifest.json host_permissions for https://openrouter.ai/*
  - 4 unit tests for OpenRouter provider error/success paths

affects:
  - 02.1-02 (options UI for openrouter selection and model entry)
  - any future plan that reads ProviderConfig.provider type

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "OpenRouter uses OpenAI-compatible chat completions format with additional HTTP-Referer and X-Title ranking headers"
    - "Dynamic model list providers use empty array in PROVIDER_MODELS; options UI handles this via length check before populating select"

key-files:
  created:
    - src/providers/openrouter.ts
  modified:
    - src/types.ts
    - src/background.ts
    - manifest.json
    - test/unit.mjs
    - src/options.ts

key-decisions:
  - "openrouter PROVIDER_MODELS entry is empty array [] because models are fetched dynamically (not hardcoded like openai/gemini)"
  - "Include HTTP-Referer and X-Title headers in OpenRouter requests per OpenRouter ranking conventions"
  - "options.ts updateModelOptions guards models[0] access with length check to handle empty model lists"

patterns-established:
  - "Provider implementation pattern: mirror OpenAI provider exactly (AbortController, classifyError, stripWrappers)"
  - "Unit tests use withFetch helper and BASE_CONFIG from existing test infrastructure"

requirements-completed:
  - PROV-05

# Metrics
duration: 15min
completed: "2026-05-25"
---

# Phase 02.1 Plan 01: OpenRouter Provider Implementation Summary

**OpenRouterProvider class wired into hime extension using OpenAI-compatible chat completions at openrouter.ai, with 33 passing unit tests**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-25T04:58:00Z
- **Completed:** 2026-05-25T05:04:25Z
- **Tasks:** 2 of 2
- **Files modified:** 6

## Accomplishments

- Created `src/providers/openrouter.ts` implementing `TranslationProvider` exactly mirroring the OpenAI provider pattern (10s timeout, `classifyError`, `stripWrappers`, OpenAI-compatible body format)
- Extended `ProviderConfig.provider` type union to include `'openrouter'` and added `openrouter: []` to `PROVIDER_MODELS` for dynamic model support
- Registered `OpenRouterProvider` in background.ts provider registry and updated endpoint logging; added `https://openrouter.ai/*` to manifest host_permissions
- Added 4 OpenRouter unit tests (401 auth error, 429 rate limit, success quote-stripping, network TypeError) - all 33 tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Create OpenRouterProvider and update types** - `3766695` (feat)
2. **Task 2: Add OpenRouter unit tests** - `05e2666` (feat)

## Files Created/Modified

- `src/providers/openrouter.ts` - OpenRouterProvider class implementing TranslationProvider with OpenAI-compatible format, HTTP-Referer/X-Title headers, 10s timeout
- `src/types.ts` - Extended ProviderConfig.provider union to include 'openrouter'; added openrouter: [] to PROVIDER_MODELS
- `src/background.ts` - Added OpenRouterProvider import and registry entry; updated endpoint logging for openrouter
- `manifest.json` - Added https://openrouter.ai/* to host_permissions
- `test/unit.mjs` - Added OpenRouterProvider import and 4 test cases
- `src/options.ts` - Fixed updateModelOptions to guard models[0] access with length check; updated saveSettings type cast to include 'openrouter'

## Decisions Made

- `PROVIDER_MODELS.openrouter` is `[]` (empty array) because OpenRouter supports hundreds of models fetched dynamically — Plan 02 (options UI) will provide a free-text model input instead of a dropdown for openrouter
- OpenRouter ranking headers (`HTTP-Referer: https://github.com/benfoden/hime`, `X-Title: hime`) included per OpenRouter API conventions to improve model routing quality

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed options.ts TypeScript errors from empty PROVIDER_MODELS.openrouter**
- **Found during:** Task 2 (running npm test triggered tsc compilation)
- **Issue:** Adding `openrouter: []` to `PROVIDER_MODELS as const` caused TypeScript to infer `models[0]` as potentially `undefined` in `updateModelOptions()` (lines 58-59). Also `saveSettings()` type cast `as 'openai' | 'gemini'` excluded openrouter.
- **Fix:** Guarded `models[0]` access with `if (models.length > 0)` check; updated `saveSettings` type cast to `as 'openai' | 'gemini' | 'openrouter'`
- **Files modified:** `src/options.ts`
- **Verification:** `npx tsc --noEmit` exits 0; `npm test` exits 0 with 33 passing tests
- **Committed in:** `05e2666` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug)
**Impact on plan:** Auto-fix required for TypeScript correctness, triggered directly by the empty array addition in PROVIDER_MODELS. No scope creep.

## Issues Encountered

- The `npm test` command in the plan's verify step (`cd /home/ben/code/hime && npm test`) pointed to the main repo but execution runs from the worktree. This was resolved by running tests from the worktree working directory, which uses the worktree's `dist/` output from `tsc`.

## User Setup Required

None - no external service configuration required. OpenRouter API key will be entered by users in the options page (addressed in Plan 02).

## Next Phase Readiness

- OpenRouterProvider is fully implemented and registered; background service worker dispatches to it correctly
- Plan 02 (options UI) can now add the openrouter provider option and model text input to the settings page
- All existing tests continue to pass; 4 new OpenRouter tests added

## Known Stubs

None - `PROVIDER_MODELS.openrouter: []` is intentional (dynamic models); the empty array is handled in options.ts. Plan 02 will add the model text input for openrouter users.

---
*Phase: 02.1-openrouter-provider-support*
*Completed: 2026-05-25*
