---
milestone: v1.4
audited: 2026-06-26
status: tech_debt
scores:
  requirements: 13/13
  phases: 2/2
  integration: 10/10
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 16-in-place-image-overlay-translation
    items:
      - "Missing 16-VERIFICATION.md — phase was human-verified live (16-05 checkpoint, OVL-01..05 approved) and shipped in PR #6, but the formal goal-backward VERIFICATION.md doc was never written."
      - "OVL-01 robustness: translateImageBlocks dispatch (content.ts:1610) omits tabId; background falls back to tabId ?? 0 → chrome.tabs.get(0) throws for fetch-blocked/tainted/blob: images, surfacing an error-toast increment instead of a clean silent skip. Non-breaking (such images are intentionally skipped anyway). Trivial fix: use sender.tab?.id as fallback in background.ts."
  - phase: 15-in-place-page-text-translation-triggers
    items:
      - "Nyquist: 15-VALIDATION.md status=planned, wave_0_complete=false (nyquist_compliant=true but wave 0 not run)."
  - phase: 16-in-place-image-overlay-translation-nyquist
    items:
      - "Nyquist: 16-VALIDATION.md status=draft, nyquist_compliant=false, wave_0_complete=false."
nyquist:
  compliant_phases: []
  partial_phases: ["15", "16"]
  missing_phases: []
  overall: partial
deferred:
  - "One-off site image-positioning bug (exotic layout — transformed/scaled ancestor or srcset) — investigate if recurs."
  - "Below-fold image overlay on scroll (currently initial-viewport only)."
  - "Unified read-vs-compose direction model (replaces popup-swap reliance)."
  - "Backlog 999.4: per-image numbering badge/sidebar (post-v1.4)."
---

# Milestone v1.4 — In-Place Page Translation — Audit Report

**Milestone:** In-Place Page Translation (Phases 15-16) · started 2026-06-22 · shipped PR #6 (squash-merged to main)
**Audited:** 2026-06-26
**Status:** TECH_DEBT — all requirements satisfied, no critical blockers; accumulated doc/Nyquist debt + one minor robustness warning to review.

## Requirements Coverage — 13/13 satisfied

3-source cross-reference (REQUIREMENTS.md traceability `[x]` + SUMMARY `requirements_completed` frontmatter + integration-checker wiring) agrees for every REQ-ID. No unsatisfied, no orphaned requirements.

| Req | Phase | Traceability | SUMMARY | Integration | Final |
| --- | --- | --- | --- | --- | --- |
| PAGE-01..05 | 15 | [x] | listed | WIRED | satisfied |
| TRIG-01..03 | 15 | [x] | listed | WIRED | satisfied |
| OVL-01..05 | 16 | [x] | listed | WIRED | satisfied |

## Phases — 2/2

- **Phase 15** — 15-VERIFICATION.md `status: passed`, 8/8 must-haves, human UAT approved ("looks good — continue").
- **Phase 16** — No formal 15-style VERIFICATION.md, BUT human-verified live (16-05 human-verify checkpoint: OVL-01..05 approved) and shipped in PR #6. Substance verified; only the doc artifact is missing (tech debt).

## Cross-Phase Integration — 10/10 wired

Static read of `src/` (typecheck `tsc --noEmit` → 0 errors; no SW runtime, per project memory).

- Single `translatePage` handler runs `overlayRemoveAll()` → `progressReset()` → `pageTranslate()` + `overlayTranslateImages()` — text and image passes independent and additive, no clobber (content.ts:2744-2752).
- Shared BYOK background pipeline: `translatePageBatch` (background.ts:747) + `translateImageBlocks` (background.ts:806) both read keys from storage only, reuse prompt/parse helpers + 45s timeout. No contention.
- Shared language gate `progShouldGateByLanguage` mirrored once, reused by progressive + banner boot (content.ts:2816/2832/2868). Image path gated by `includeImages` (correct — image source-text may differ from `<html lang>`).
- The one deliberate P15↔P16 seam — OVL-03 swap driven by phase-15 `pageApplyState` → `overlayApplyGlobal` in lockstep (content.ts:1920) — verified WIRED.
- Message union complete (types.ts:146-174); background `default` → "Unknown message type", no silent drops.

## E2E Flows — 2/2 complete

- **(a)** foreign page → auto-offer banner (TRIG-02/03) → translate text in place (PAGE-01/02/04/05) → toggle back (PAGE-03). COMPLETE.
- **(b)** translate images → overlays anchored via boundingPoly (OVL-01/02/05) → swap overlay ↔ original (OVL-03) → reposition on scroll/resize (OVL-04). COMPLETE.

## Tech Debt (no blockers)

1. **16-VERIFICATION.md missing** — verified live + shipped, doc never written.
2. **OVL-01 tabId fallback** — `translateImageBlocks` omits `tabId`; background `tabId ?? 0` → `chrome.tabs.get(0)` throws on fetch-blocked/tainted/blob: images, yielding an error-toast instead of a silent skip. Non-breaking; trivial fix (use `sender.tab?.id`).
3. **Nyquist partial** — 15-VALIDATION planned/wave_0 incomplete; 16-VALIDATION draft/non-compliant.

## Deferred (carried to backlog / future milestone)

- One-off site image-positioning bug (exotic transformed/scaled ancestor or srcset) — investigate if recurs.
- Below-fold image overlay on scroll (currently initial-viewport only).
- Unified read-vs-compose direction model (replaces popup-swap reliance).
- Backlog 999.4: per-image numbering badge/sidebar (post-v1.4).
