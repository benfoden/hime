---
phase: 13-progressive-viewport-mode-cost-control-privacy-opt-in
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/options.ts
  - src/options.html
  - src/options.css
autonomous: false
requirements: [PROG-01, PROG-05]
must_haves:
  truths:
    - "Progressive image translation is OFF by default (DEFAULT_SETTINGS.progressiveEnabled === false)."
    - "The options page shows a progressive-mode toggle the user can flip; saving persists it and it takes effect without an extension reload."
    - "Turning the toggle ON for the FIRST time shows a blocking acknowledgement modal naming BOTH destinations (image bytes → Google Cloud Vision; OCR'd text → the user's configured LLM provider)."
    - "Acknowledgement is remembered (storage.local progressiveAck) so a later enable does not re-prompt."
    - "Declining the modal leaves progressive OFF; the toggle reverts."
  artifacts:
    - path: "src/types.ts"
      provides: "progressiveEnabled settings field + the full Phase-13 message-type contract consumed by Plans 03/04"
      contains: "progressiveEnabled"
    - path: "src/options.html"
      provides: "Progressive section: toggle + first-enable modal markup + status region"
      contains: "progressiveEnabled"
    - path: "src/options.ts"
      provides: "Toggle wiring, first-enable blocking modal, storage.local ack persistence, live-effect save"
      contains: "progressiveAck"
    - path: "src/options.css"
      provides: "Modal + toggle styling"
      min_lines: 10
  key_links:
    - from: "src/options.ts"
      to: "chrome.storage.local himeSettings.progressiveEnabled"
      via: "saveSettings writes the toggle value"
      pattern: "progressiveEnabled"
    - from: "src/options.ts"
      to: "chrome.storage.local progressiveAck"
      via: "first-enable modal acknowledgement persisted separately"
      pattern: "progressiveAck"
---

<objective>
Add the progressive-mode settings surface and lock the full Phase-13 type contract.
This plan owns ALL of `types.ts` for the phase (interface-first ordering): it adds
the `progressiveEnabled` settings field (default false, PROG-01) AND every new
message type Plans 03/04 will send/handle, so the downstream browser-only files
import a finished contract instead of editing types.ts in a later wave. It also
builds the options UI: the toggle (PROG-01), the blocking first-enable privacy
modal that names BOTH upload destinations (PROG-05 / D-03), and the remembered
acknowledgement (D-03).

Purpose: Progressive mode is privacy-sensitive auto-upload — it MUST be opt-in,
default-OFF, and gated behind an explicit one-time consent that is honest about
where bytes and OCR'd text go (two destinations, per the Phase 12 OCR→LLM steer).

Output: type contract in `types.ts`; toggle + modal + ack in
`options.{ts,html,css}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-progressive-viewport-mode-cost-control-privacy-opt-in/13-CONTEXT.md

# Settings + message-type definitions to extend (Settings interface, DEFAULT_SETTINGS,
# MessageType union, the *Message interfaces, migrateSettings).
@src/types.ts

# Options patterns to clone: Image Translation / Translated Search sections (markup),
# saveSettings / populateForm / showStatus / DOMContentLoaded wiring, btn-secondary +
# status conventions, the testVisionKey "save-to-storage-first" precedent.
@src/options.ts
@src/options.html
@src/options.css
</context>

<artifacts_this_phase_produces>
NEW symbols introduced here (consumed by Plans 03 + 04):

| Symbol | File | Kind | Meaning |
|--------|------|------|---------|
| `progressiveEnabled` | types.ts Settings | boolean field, default false | PROG-01 master toggle |
| `'progressiveTranslate'` | types.ts MessageType | message type | content → worker: enqueue a progressive image job (gated, content-hash) |
| `'openImagePanel'` | types.ts MessageType | message type | content → worker: badge-click gesture asks worker to sidePanel.open + scroll to entry (D-04, PROG-06) |
| `'progressiveActivity'` | types.ts MessageType | message type | worker → content (and toolbar): activity count update (pending+done, D-04a) |
| `ProgressiveTranslateMessage` | types.ts | interface | payload `{ srcUrl: string; dedupKey: string; tabId?: number }` (NO keys — T-12-01 law) |
| `OpenImagePanelMessage` | types.ts | interface | payload `{ tabId: number; dedupKey: string }` (gesture-backed open + scroll target) |
| `ProgressiveActivityMessage` | types.ts | interface | payload `{ pending: number; done: number }` |
| `STORAGE_PROGRESSIVE_ACK` | types.ts | const string `'progressiveAck'` | storage.local key for the one-time consent (D-03) |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the type contract in src/types.ts (settings field + Phase-13 messages)</name>
  <read_first>
    - @src/types.ts — Settings interface (lines 18-30), DEFAULT_SETTINGS (lines 332-347), migrateSettings (lines 350-357), MessageType union (lines 126-143), the TranslateImageMessage / SearchTranslatedMessage interface shapes to clone, and the T-12-01 "key never in a message" doctrine in the TranslateImageMessage comment.
    - @.planning/phases/13-progressive-viewport-mode-cost-control-privacy-opt-in/13-CONTEXT.md — D-03 (ack remembered), D-04 (badge-click gesture → open + scroll), D-04a (activity count), PROG-06 (never auto-open).
  </read_first>
  <action>
    In src/types.ts: (1) add `progressiveEnabled: boolean` to the Settings interface with a comment citing PROG-01 / D-01 (default OFF, takes effect without reload). (2) Set `progressiveEnabled: false` in DEFAULT_SETTINGS (PROG-01 default-OFF). migrateSettings already spreads DEFAULT_SETTINGS over raw, so legacy settings get false automatically — confirm no extra migration code is needed. (3) Add `'progressiveTranslate'`, `'openImagePanel'`, `'progressiveActivity'` to the MessageType union. (4) Add the three interfaces from the artifacts table (ProgressiveTranslateMessage / OpenImagePanelMessage / ProgressiveActivityMessage), cloning TranslateImageMessage's comment doctrine — explicitly note the BYOK key is NEVER in any payload (T-12-01) and progressive payloads carry only {srcUrl, dedupKey, tabId?}-shaped data per the content-script rendering law in 13-CONTEXT. (5) Export `STORAGE_PROGRESSIVE_ACK = 'progressiveAck'` for the one-time consent flag (D-03), with a comment that the ack lives in storage.local (Claude's-discretion in 13-CONTEXT: ack → storage.local, per-page counter → storage.session). Keep additions adjacent to the existing image types so the file reads cohesively. Strict types, no `any`.
  </action>
  <verify>
    <automated>npm run build && node -e "import('./dist/types.js').then(m=>{const s=m.DEFAULT_SETTINGS; if(s.progressiveEnabled!==false) throw new Error('progressiveEnabled must default false'); if(m.STORAGE_PROGRESSIVE_ACK!=='progressiveAck') throw new Error('ack const missing'); console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "progressiveEnabled: false" src/types.ts` (default OFF).
    - `grep -q "progressiveTranslate" src/types.ts && grep -q "openImagePanel" src/types.ts && grep -q "progressiveActivity" src/types.ts` (all three message types in the union).
    - `grep -q "STORAGE_PROGRESSIVE_ACK" src/types.ts`.
    - `npm run build` is clean (tsc strict); no `: any` added.
    - No payload interface contains an apiKey/googleApiKey field (T-12-01).
  </acceptance_criteria>
  <done>The full Phase-13 type contract exists; Plans 03/04 can import it without touching types.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Progressive section — toggle, first-enable blocking modal, ON-effect save</name>
  <read_first>
    - @src/options.html — the `<section>` pattern (Image Translation lines 64-75 / Translated Search lines 51-62), the help-text + status-div conventions, the `<script type="module">` tail.
    - @src/options.ts — populateForm (lines 119-134), saveSettings (lines 217-244), showStatus (lines 430-440), DOMContentLoaded element-grab + listener wiring (lines 443-509), the testVisionKey save-to-storage-first precedent (lines 340-372).
    - @src/options.css — existing form-group / btn-secondary / status / help-text tokens to reuse.
  </read_first>
  <action>
    Add a new "Progressive Image Translation" `<section>` to options.html with: a checkbox/toggle input `#progressiveEnabled` (label "Auto-translate images as you scroll (opt-in)"), help-text stating it is OFF by default and that page images you pause near are uploaded; a hidden blocking modal (`#progressiveModal`, `role="dialog"`, `aria-modal="true"`) containing the privacy copy and Enable / Cancel buttons; and a status div `#progressiveStatus`. The modal COPY MUST name BOTH destinations explicitly (D-03 / PROG-05): "image bytes are uploaded to Google Cloud Vision (OCR)" AND "the detected text is then sent to your configured translation provider (your selected LLM)". Markup is static HTML (textContent-only law applies to the page-context badge in Plan 03, not this trusted options page — but keep copy in HTML, not innerHTML-injected). In options.ts: grab the new elements in DOMContentLoaded; in populateForm set the toggle checked = currentSettings.progressiveEnabled; in saveSettings persist `progressiveEnabled: progressiveToggle.checked`. Wire the toggle `change` handler: when the user turns it ON, read storage.local STORAGE_PROGRESSIVE_ACK — if NOT acknowledged, show the blocking modal and revert the checkbox to unchecked until they confirm; on Enable click, set storage.local[STORAGE_PROGRESSIVE_ACK]=true, hide modal, re-check the toggle, and save settings so it takes effect immediately (PROG-01 no-reload — content.ts already watches chrome.storage.onChanged for himeSettings, so a save propagates live); on Cancel click, hide modal, leave toggle OFF (D-03 decline → stays off). If ack already true, enabling skips the modal. Add minimal modal + toggle CSS to options.css (overlay, centered dialog, button row) reusing existing color tokens.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');const h=fs.readFileSync('dist/options.html','utf8');const j=fs.readFileSync('dist/options.js','utf8');for(const t of ['progressiveEnabled','progressiveModal','Google Cloud Vision']) if(!h.includes(t)) throw new Error('html missing '+t);if(!j.includes('progressiveAck')) throw new Error('options.js missing ack handling');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - options.html contains `#progressiveEnabled` toggle, `#progressiveModal` dialog, and modal copy naming BOTH "Google Cloud Vision" and the configured translation/LLM provider (D-03).
    - options.js persists `progressiveEnabled` in saveSettings and reads/writes `progressiveAck` in storage.local.
    - First enable with no ack shows the modal and keeps the toggle OFF until Enable is clicked; Cancel leaves it OFF.
    - A second enable after ack does NOT re-show the modal.
    - `npm run build` clean; no `: any` introduced.
  </acceptance_criteria>
  <done>User can opt in via a remembered, two-destination consent flow; the setting persists and propagates live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Progressive Image Translation options section: default-OFF toggle, first-enable blocking modal naming both upload destinations (Google Cloud Vision + the configured LLM provider), remembered acknowledgement, and live-effect save.</what-built>
  <how-to-verify>
    1. Run `npm run build`, then load/reload the unpacked extension from `dist/` in Chrome (chrome://extensions → reload).
    2. Open the extension Options page. Confirm a "Progressive Image Translation" section exists and the toggle is OFF by default.
    3. Flip the toggle ON → a blocking modal must appear. Read its copy: it MUST mention BOTH (a) image bytes going to Google Cloud Vision and (b) detected text going to your configured translation/LLM provider.
    4. Click Cancel → modal closes, toggle stays OFF.
    5. Flip ON again → modal reappears (still unacknowledged). Click Enable → modal closes, toggle is ON, Save shows success.
    6. Reload the Options page, flip the toggle OFF then ON again → modal must NOT reappear (ack remembered).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what was wrong with the toggle/modal/ack behavior or copy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → consent state | The progressiveAck flag authorizes silent auto-upload of page images; it must require an explicit, informed gesture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Repudiation/Info-disclosure | enabling auto-upload without informed consent | mitigate | Blocking first-enable modal naming BOTH destinations (D-03/PROG-05); default OFF; decline keeps it OFF. Pinned by the human-verify checkpoint. |
| T-13-04 | Tampering | progressiveAck stored client-side | accept | Same trust model as existing himeSettings in storage.local; no privilege escalation — worst case re-shows a consent dialog. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (HTML/CSS/TS edits only). No install task → no slopcheck gate needed. |
</threat_model>

<verification>
- `npm run build` clean.
- DEFAULT_SETTINGS.progressiveEnabled === false.
- options.html + options.js contain the toggle, modal (both destinations), and ack handling.
- Human-verify checkpoint passes (toggle/modal/ack behavior + copy correct).
</verification>

<success_criteria>
- PROG-01: default-OFF toggle that takes effect without reload (live via storage.onChanged).
- PROG-05: first-enable blocking modal naming both upload destinations; acknowledgement remembered.
- The full Phase-13 message-type contract is defined for Plans 03/04 to consume.
</success_criteria>

<output>
Create `.planning/phases/13-progressive-viewport-mode-cost-control-privacy-opt-in/13-02-SUMMARY.md` when done.
</output>
