---
phase: 14-ux-quality-hardening-vision-settings
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vision-google.ts
  - src/options.ts
  - test/vision-google.mjs
autonomous: false
requirements: [VIS-02]
must_haves:
  truths:
    - "The Google Cloud key field + connection-test UI exists and routes the test through the background worker (key never in the message payload)"
    - "The connection test exercises the Vision endpoint through the worker, and the SC#1 wording is reconciled to current behavior (Vision via the Google key; translation via the configured LLM pipeline)"
  artifacts:
    - path: "src/options.ts"
      provides: "verified testVisionKey handler (no rebuild) + reconciled comment/copy on what the test exercises"
      contains: "testVisionKey"
    - path: "src/vision-google.ts"
      provides: "verified testConnection (Vision-only probe) with a reconciled doc comment matching SC#1"
      contains: "testConnection"
  key_links:
    - from: "src/options.ts"
      to: "background testVisionKey case"
      via: "payload-less chrome.runtime.sendMessage({ type: 'testVisionKey' })"
      pattern: "testVisionKey"
    - from: "background testVisionKey"
      to: "vision-google.testConnection"
      via: "worker probe with key read from storage"
      pattern: "testConnection"
---

<objective>
Verify (NOT rebuild) the VIS-02 Google Cloud key field + connection test that already landed (commits 0a07344, fb9395e, 269316a) and reconcile the SC#1 wording against current behavior. SC#1 says the test "exercises the Vision + Translation endpoints through the background worker", but the pipeline moved translation off the Google key onto the configured LLM model (Phase 12 steer): `vision-google.ts` `testConnection` now probes Vision ONLY. This plan confirms the worker-mediated, key-stays-in-storage test path is correct, adds a regression test asserting the Vision-only probe contract, and fixes any stale "Vision + Translation v2" wording in the test path's comments/UI copy so the docs match what the code actually does. It does NOT re-plan or rebuild the key input UI.

Purpose: VIS-02 is verify-only — the user explicitly scoped this as confirmation, not rebuild. The one real correction is reconciling the SC#1 "Vision + Translation through the worker" phrasing with the current Vision-only probe so the connection test does not mislead the user into thinking the Google key validates translation (which now runs through their LLM provider).
Output: a node test asserting `testConnection` probes only the Vision endpoint (no translation call) and surfaces classified errors with key-free messages; corrected doc comments / UI copy in `vision-google.ts` + `options.ts`; a verification record in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-ux-quality-hardening-vision-settings/14-CONTEXT.md
@.planning/phases/14-ux-quality-hardening-vision-settings/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify + regression-test the Vision-only connection probe; reconcile SC#1 wording</name>
  <files>src/providers/vision-google.ts, src/options.ts, test/vision-google.mjs</files>
  <read_first>
    - src/providers/vision-google.ts (`testConnection` lines 113-133 — Vision-only probe with the 1×1 `PROBE_PNG_BASE64`; the doc comment lines 113-121 already says "Probes the Vision endpoint only — translation no longer runs through this key"; `postJson` error shape lines 143-177; `VISION_ENDPOINT`)
    - src/options.ts (`testVisionKey` lines 350-382 — saves the key to storage first, then sends a payload-less `testVisionKey` message; `googleApiKeyInput`/`testVisionKeyBtn`/`visionTestStatusDiv` element refs lines 17-19; NOTE the stale comment lines 345-349 still says "probes BOTH Vision + Translation v2" — this is the wording to reconcile)
    - src/background.ts (`testVisionKey` case lines 721-740 — reads key from storage, calls `visionProvider.testConnection(apiKey)`; the stale comment lines 722-724 says "Probes both Vision + Translation v2")
    - test/vision-google.mjs (existing node:test harness for the provider — extend with a testConnection case)
    - .planning/REQUIREMENTS.md VIS-02 + .planning/ROADMAP.md §"Phase 14" SC#1 (the wording being reconciled)
    - .planning/phases/14-ux-quality-hardening-vision-settings/14-PATTERNS.md §`src/options.ts` (VERIFY ONLY — do NOT re-plan the key UI) and §`src/vision-google.ts` / §`src/background.ts` testVisionKey (reconcile note)
  </read_first>
  <action>
    Do NOT rebuild the key UI. First VERIFY the existing path: confirm `options.ts testVisionKey` saves the key to storage then sends a PAYLOAD-LESS `{ type: 'testVisionKey' }` (key never in the message), that `background.ts` reads `settings.googleApiKey` from storage and calls `visionProvider.testConnection`, and that `testConnection` probes ONLY `VISION_ENDPOINT` (no translation fetch) and rejects with a classified Error carrying `.kind`/`.status` and NO key in the message. Then RECONCILE the stale wording: update the comments in `options.ts` (lines 345-349), `background.ts` (lines 722-724), and the `testConnection` doc comment in `vision-google.ts` so they accurately state the test exercises the Cloud Vision endpoint through the worker (the Google key needs ONLY Cloud Vision enabled; translation runs through the user's configured LLM provider, not this key). Keep the change to comments/copy only — no behavioral change to the probe. Add a regression test in `test/vision-google.mjs` that stubs `fetch` and asserts: (a) `testConnection` issues exactly ONE request and it targets the Vision endpoint (never a translation.googleapis.com URL), (b) a 403 response rejects with a classified message that does NOT contain the API key string, (c) a 200 resolves. If, during verification, the worker-mediated/key-in-storage invariant is found BROKEN (key leaked into a payload, or a translation call snuck into the probe), STOP and record it as a blocker in the SUMMARY rather than silently patching scope — but the expected outcome is that the path is correct and only the wording needs reconciling.
  </action>
  <verify>
    <automated>npm run build && node --test test/vision-google.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Vision \+ Translation|Translation v2" src/options.ts src/background.ts src/providers/vision-google.ts` returns NO stale "test exercises Translation" claims in the testVisionKey path (the wording is reconciled to Vision-only-through-the-worker + LLM-translation).
    - `test/vision-google.mjs` has a new testConnection case asserting: exactly one fetch, Vision endpoint only (no translation.googleapis.com), 403→classified key-free rejection, 200→resolve.
    - `rg -n "type: 'testVisionKey'" src/options.ts` confirms the message is payload-less (no key field) — the worker-mediated invariant is intact.
    - `node --test test/vision-google.mjs` passes; `npm run build` exits 0.
    - No new key-UI elements were added to options.ts/options.html (grep: the element refs `googleApiKeyInput`/`testVisionKeyBtn` are unchanged in count — this is verify-only).
  </acceptance_criteria>
  <done>The VIS-02 connection-test path is verified worker-mediated + key-in-storage, the SC#1 wording is reconciled to Vision-only (LLM translation), and a regression test pins the Vision-only probe; build + tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify VIS-02 live connection test (valid + invalid key) and reconciled copy</name>
  <action>Pause for the human reviewer. Follow the <how-to-verify> steps against a live Google Cloud key in the options page; this confirms the chrome.runtime message round-trip and live endpoint behavior the node harness cannot exercise. Do not proceed until the resume-signal is given.</action>
  <what-built>
    Verification that the already-shipped VIS-02 key field + connection test (options page) works against a real Google Cloud key through the background worker, with the SC#1 wording reconciled to match the Vision-only probe. The automated test pins the probe contract; this checkpoint confirms the live options-page UX with a real key (the node harness cannot exercise the chrome.runtime message round-trip or a live Google endpoint).
  </what-built>
  <how-to-verify>
    1. Run `npm run build` and reload the unpacked `dist/` extension; open the options page.
    2. Enter a VALID Google Cloud API key (Cloud Vision API enabled) in the Vision/OCR key field and click the connection-test button → confirm it reports success ("Vision key valid!").
    3. Enter an INVALID/empty key and re-test → confirm it reports a clear failure naming the reason (auth/unauthorized), with no raw key echoed.
    4. Confirm the surrounding copy/labels no longer claim the test validates "Translation v2" — the user should understand the Google key is for Vision OCR and translation uses their configured LLM model.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (test passes with a bad key, failure reason unclear, copy still mentions Translation v2).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| options page → background worker | The Google key is saved to storage then validated via a payload-less worker message — the key must never travel in the message. |
| Google Vision response → worker → options | The probe's error body is surfaced to the options status line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Information Disclosure | testVisionKey message + error surfacing | mitigate | The key is read from storage in the worker, never carried in the `testVisionKey` payload (T-12-01); the regression test asserts the rejection message contains no key string; errors.ts already keeps the key out of classified messages (key lives only in URL ?key=). |
| T-14-13 | Spoofing | a "valid" report from a probe that only hits Vision | mitigate | Reconciled copy makes the scope explicit (Vision-only; LLM handles translation) so a passing test does not falsely imply translation is validated — closes the misleading-success gap SC#1 implied. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; verify + comment/test edits only. |
</threat_model>

<verification>
- `node --test test/vision-google.mjs` passes the new testConnection contract cases.
- `npm run build` exits 0.
- Grep confirms no stale "Translation v2" test-path wording and a payload-less testVisionKey message.
- Human verifies the live options connection test (valid + invalid key) and the reconciled copy.
</verification>

<success_criteria>
- The connection-test path is confirmed worker-mediated with the key in storage only.
- SC#1 wording is reconciled to Vision-only probe + LLM-pipeline translation.
- A regression test pins the Vision-only contract.
- The key UI is NOT rebuilt (verify-only honored).
</success_criteria>

<output>
Create `.planning/phases/14-ux-quality-hardening-vision-settings/14-05-SUMMARY.md` when done.
</output>
