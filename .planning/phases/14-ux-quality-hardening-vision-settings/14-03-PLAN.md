---
phase: 14-ux-quality-hardening-vision-settings
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/panel-render.ts
  - src/sidepanel.css
  - src/panel-mock.ts
  - test/panel-render.mjs
autonomous: true
requirements: [IMG-06]
must_haves:
  truths:
    - "Every panel entry shows a [hime N] number that matches its on-image badge"
    - "A populated entry whose result is CJK/vertical shows a small per-result quality note; non-CJK results do not"
    - "A failure card is visually distinct (amber/red, reason-naming) from the neutral muted no-text card"
    - "A populated entry has a Copy button (node) for the translation and a 'show original' toggle revealing the original with its own copy node"
  artifacts:
    - path: "src/panel-render.ts"
      provides: "[hime N] chip, CJK note, copy button + show-original toggle nodes, reason-bearing error card"
      contains: "panel-num"
    - path: "src/sidepanel.css"
      provides: ".panel-num, .panel-note, .panel-copy, .panel-original collapsed state, divergent .panel-error vs .panel-no-text styling"
      contains: ".panel-num"
  key_links:
    - from: "src/panel-render.ts"
      to: "ImageEntry.himeNum / verticalOrCjk"
      via: "entryEl prepend + populated-branch conditional append"
      pattern: "himeNum|verticalOrCjk"
    - from: "src/panel-render.ts"
      to: "navigator.clipboard target string"
      via: "data attribute carrying entry.result.translatedText/originalText for the host handler"
      pattern: "data-copy"
---

<objective>
Land the panel-render and CSS half of Phase 14's visible affordances, all textContent-only via the `el()` helper (IMG-02 law): the `[hime N]` chip on every entry (D-04), the per-result CJK/vertical quality note on flagged populated entries only (D-03), a failure card that is unmistakably distinct from the neutral no-text card and names its reason (D-02), and the Copy button + "show original" toggle NODES for IMG-06 (D-01). The clipboard CLICK handler is browser-only and is wired in 14-04 (sidepanel.ts); this plan builds the nodes and carries the copy-source strings on data attributes so the host can read them, keeping panel-render.ts node-testable (no `navigator`).

Purpose: panel-render.ts is the single sanctioned render path. All four decisions extend the existing `entryEl` switch by mirroring sibling patterns (the lowConfidence badge, the existing error/no-text branches) rather than inventing a new renderer.
Output: extended `entryEl` (chip + note + copy/show-original nodes + reason-bearing error message), new CSS classes (`.panel-num`, `.panel-note`, `.panel-copy`, collapsed `.panel-original`) with the error/no-text visual split sharpened, new panel-render node tests, and supporting panel-mock fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ux-quality-hardening-vision-settings/14-CONTEXT.md
@.planning/phases/14-ux-quality-hardening-vision-settings/14-PATTERNS.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [hime N] chip + CJK note + reason-bearing failure card (panel-render.ts + sidepanel.css)</name>
  <files>src/panel-render.ts, src/sidepanel.css, test/panel-render.mjs</files>
  <read_first>
    - src/panel-render.ts (the `el()` helper lines 71-81 — the ONLY sanctioned text path; `entryEl` switch lines 98-159; the `populated` branch lines 107-135 with the `lowConfidence` badge append lines 130-133; the `no-text` branch lines 136-142; the `error` branch lines 143-150 with `data-error-kind` + `panel-message`)
    - src/types.ts (the 14-01 fields: `himeNum?` on all kinds, `verticalOrCjk?` on populated; `error` variant carries `errorKind` + `message`)
    - src/sidepanel.css (`.panel-direction` lines 93-97; `.panel-badge`/`.panel-badge.amber` lines 125-141; `.panel-message`/`.panel-no-text .panel-message` lines 170-179; `.panel-error .panel-message` lines 181-190 — the existing red error box)
    - .planning/phases/14-ux-quality-hardening-vision-settings/14-PATTERNS.md §`src/panel-render.ts` (D-04 chip prepended on EVERY kind; D-03 note mirrors the lowConfidence badge; D-02 do NOT collapse error vs no-text — sharpen the CSS divergence and ensure the message names the reason) and §`src/sidepanel.css` (new classes following existing tokens; `.panel-note` like `.panel-direction`; `.panel-num` like the content-script badge style)
  </read_first>
  <action>
    In `entryEl`, prepend a `[hime N]` chip on EVERY entry kind (it correlates the row to the on-image badge): build `el(doc, 'span', { text: \`[hime ${entry.himeNum}]\`, className: 'panel-num' })` and append it first (or guard when `himeNum` is undefined to keep legacy entries safe — render the chip only when `himeNum != null`). In the `populated` branch, add a sibling conditional AFTER the lowConfidence badge: `if (entry.verticalOrCjk) row.appendChild(el(doc, 'div', { text: 'vertical/CJK text — OCR may be imperfect', className: 'panel-note' }))` — D-03, textContent-only, note appears ONLY where the flag is set (no standing disclaimer). For D-02, keep the `error` and `no-text` branches separate (do NOT merge); the error branch already renders `entry.message` via textContent — confirm the message is the worker's reason-naming string (from 14-01) and leave the rendering as-is. In `sidepanel.css`: add `.panel-num` (small monospace chip mirroring the content-script badge tokens — `font-family: monospace`, small font, muted/blue background, inline-block, like `content.ts:1084-1096`); add `.panel-note` (small, muted, italic-ish, modeled on `.panel-direction`); and SHARPEN the error-vs-no-text divergence so the failure card reads as clearly-broken (amber/red — the existing `.panel-error .panel-message` red box at lines 183-190 stays, optionally add a left border / icon affordance) and the `.panel-no-text .panel-message` stays neutral muted italic (lines 177-179). Do not introduce innerHTML anywhere.
  </action>
  <verify>
    <automated>npm run build && node --test test/panel-render.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "panel-num|verticalOrCjk|panel-note" src/panel-render.ts` shows the chip prepend (guarded on himeNum) and the CJK note conditional in the populated branch.
    - `rg -n "\.panel-num|\.panel-note" src/sidepanel.css` shows both new classes; `.panel-error` and `.panel-no-text` message styles remain distinct (grep both rules present with different color/background).
    - panel-render.ts contains NO `innerHTML` and NO `navigator` reference (grep both absent).
    - `node --test test/panel-render.mjs` passes including new cases for: chip renders `[hime N]`, CJK note appears only when `verticalOrCjk` true, error card carries the reason message and is a different class than no-text.
    - `tsc` clean.
  </acceptance_criteria>
  <done>The renderer shows `[hime N]` on every entry, a CJK note only on flagged populated entries, and a reason-naming failure card visually distinct from the muted no-text card; node tests + build green.</done>
</task>

<task type="auto">
  <name>Task 2: Copy button + show-original toggle nodes (panel-render.ts + sidepanel.css + panel-mock.ts)</name>
  <files>src/panel-render.ts, src/sidepanel.css, src/panel-mock.ts, test/panel-render.mjs</files>
  <read_first>
    - src/panel-render.ts (the `populated` branch lines 107-135 — translation `.panel-translation` line 124 and original `.panel-original` lines 126-128; the `el()` helper; the NOTE in 14-PATTERNS that the click HANDLER is browser-only and belongs in sidepanel.ts — this task builds only the NODES)
    - .planning/phases/14-ux-quality-hardening-vision-settings/14-CONTEXT.md §"Copy affordance (IMG-06, SC#2)" / D-01 (ONE Copy button copies the translated text; the original is reachable via a 'show original' toggle that has its own copy path; brief 'Copied' feedback; clipboard on the STORED string; minimal markup is Claude's Discretion)
    - .planning/phases/14-ux-quality-hardening-vision-settings/14-PATTERNS.md §`src/panel-render.ts` (D-01) and §`src/sidepanel.ts` (build node here, attach clipboard click in the browser-only host)
    - src/panel-mock.ts (fixtures to extend for the new test cases)
  </read_first>
  <action>
    In the `populated` branch, append a Copy affordance and a show-original toggle, all built with `el()` (textContent-only, no innerHTML, no `navigator` in this file). Copy button: `el(doc, 'button', { text: 'Copy', className: 'panel-copy' })` carrying the copy source on a data attribute the host reads — set `button.setAttribute('data-copy', entry.result.translatedText)` (the STORED translated string, the same one already rendered at line 124) and a `data-copy-kind="translation"`. Render the original COLLAPSED by default: gate the existing `.panel-original` div behind a "show original" toggle — add a toggle control (`el(doc, 'button'/'a', { text: 'show original', className: 'panel-show-original' })`) and render the `.panel-original` block with a collapsed class (e.g. add `hidden`/`is-collapsed`) plus its OWN copy node carrying `data-copy` = `entry.result.originalText` / `data-copy-kind="original"`. Keep the markup minimal (Claude's Discretion). Do NOT attach any click listener and do NOT reference `navigator` here — the toggle reveal and the clipboard write are wired in 14-04 (sidepanel.ts) by reading these data attributes; panel-render.ts must stay node-testable. In `sidepanel.css` add `.panel-copy` (minimal button style), `.panel-show-original` (link-like), and a collapsed/hidden rule for the original block. Extend `panel-mock.ts` only if a fixture is needed for the copy-node test.
  </action>
  <verify>
    <automated>npm run build && node --test test/panel-render.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "panel-copy|data-copy|show-original" src/panel-render.ts` shows the Copy button carrying `data-copy`=translatedText and the show-original toggle + an original-copy node carrying `data-copy`=originalText.
    - panel-render.ts still contains NO `navigator` and NO `innerHTML` (grep both absent) — the file remains node-testable.
    - The original block renders collapsed by default (grep a hidden/collapsed class on the `.panel-original` element).
    - `node --test test/panel-render.mjs` passes new cases: Copy button present with translation data-copy; original block present but collapsed; original copy node carries the original-text data-copy.
    - `rg -n "\.panel-copy|\.panel-show-original" src/sidepanel.css` shows the new styles.
    - `tsc` clean.
  </acceptance_criteria>
  <done>A populated entry renders a Copy button (translation source on data-copy), a 'show original' toggle, and a collapsed original block with its own copy node — all textContent-only, no navigator in panel-render.ts; node tests + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored OCR/translation strings → DOM | Vendor-derived OCR + LLM translation text is rendered into the panel and copied to the clipboard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | OCR/translation text (incl. XSS_PROBE) rendered into entry nodes, note, chip | mitigate | All strings assigned via `el()` → `textContent` only, never innerHTML (IMG-02 law); the existing panel-render XSS test (XSS_PROBE renders inert) is extended to cover the new nodes. The `data-copy` attribute holds the raw string but `setAttribute` does not parse HTML — inert. |
| T-14-08 | Information Disclosure | copy-source strings on data-copy attributes | accept | The data is the same OCR/translation text already visibly rendered in the same entry; placing it on a data attribute exposes nothing new and never leaves the page until the user clicks Copy (host handler, 14-04). |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; no new dependency surface. |
</threat_model>

<verification>
- `node --test test/panel-render.mjs` passes the new chip / CJK-note / failure-card / copy-node cases AND the existing XSS_PROBE inert-render test still passes.
- `npm run build` exits 0 (tsc clean).
- Grep confirms no `innerHTML` / no `navigator` in panel-render.ts.
- Full `npm test` green.
</verification>

<success_criteria>
- Every entry renders `[hime N]`; CJK note appears only on flagged populated entries.
- The failure card is visibly distinct from the muted no-text card and names its reason.
- A populated entry has a Copy button (translation) and a show-original toggle with its own copy node; the original is collapsed by default.
- panel-render.ts stays node-testable (no navigator, no innerHTML).
</success_criteria>

<output>
Create `.planning/phases/14-ux-quality-hardening-vision-settings/14-03-SUMMARY.md` when done.
</output>
