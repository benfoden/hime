---
phase: 16-in-place-image-overlay-translation
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/popup.html
  - src/popup.ts
autonomous: true
requirements: [OVL-01]
must_haves:
  truths:
    - "The popup shows an 'Include images' checkbox next to the Translate page control, default OFF"
    - "Toggling the checkbox persists includeImages into himeSettings in chrome.storage.local, preserving other settings fields"
    - "On popup open the checkbox reflects the stored includeImages value (false when unset)"
  artifacts:
    - path: "src/popup.html"
      provides: "Include images checkbox markup with the Translate page control"
      contains: "includeImages"
    - path: "src/popup.ts"
      provides: "checkbox load + persist wiring"
      contains: "includeImages"
  key_links:
    - from: "src/popup.ts checkbox change handler"
      to: "chrome.storage.local himeSettings"
      via: "merge-write preserving other fields"
      pattern: "himeSettings"
---

<objective>
Add the D-01 "Include images" opt-in checkbox to the popup, sitting with the existing "Translate page" control. It loads from and persists to `himeSettings` in `chrome.storage.local`, default OFF (phase-13 default-off cost philosophy for paid BYOK Vision). This is the single user-facing switch that, when ON, makes the existing "Translate page" action ALSO overlay images — the content script (Plan 04) reads this flag at translate time and branches into the image-overlay pass.

Purpose: D-01 folds image overlay into the existing page-translate action rather than adding a separate control; the checkbox is the opt-in gate that keeps paid Vision spend off by default. Per OVL-01 / D-01.
Output: Checkbox markup in `popup.html` and load/persist wiring in `popup.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-in-place-image-overlay-translation/16-RESEARCH.md
@.planning/phases/16-in-place-image-overlay-translation/16-PATTERNS.md
@.planning/phases/16-in-place-image-overlay-translation/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include images checkbox markup + load/persist wiring</name>
  <read_first>
    - src/popup.html:20-24 (the "Translate page" button — place the checkbox markup directly under it, matching the surrounding control style)
    - src/popup.ts:1-2 (imports), :11 (translatePageBtn declaration), :21-33 (loadSettings himeSettings read — the storage shape to extend), :120-137 (DOMContentLoaded init — where element refs + listeners are wired)
    - src/types.ts Settings.includeImages + DEFAULT_SETTINGS.includeImages (from Plan 01 — the persisted field)
    - .planning/phases/16-in-place-image-overlay-translation/16-PATTERNS.md §"src/popup.ts + src/popup.html" (the loadSettings analog + merge-write-preserving-other-fields rule)
  </read_first>
  <action>
    In `src/popup.html`, add a labeled checkbox directly beneath the `#translatePage` button: `<label class="include-images"><input type="checkbox" id="includeImages"> Include images</label>` (match the existing markup style; no inline JS). In `src/popup.ts`: declare `let includeImagesCheckbox: HTMLInputElement;`; in `DOMContentLoaded` get `document.getElementById('includeImages') as HTMLInputElement` and add a `change` listener that reads the current `himeSettings`, sets `includeImages: checkbox.checked`, and writes the MERGED object back (`chrome.storage.local.set({ himeSettings: { ...current, includeImages: checkbox.checked } })` — preserve every other field). In `loadSettings`, after reading `settings`, set `includeImagesCheckbox.checked = settings.includeImages ?? false` (default OFF, D-01). Do not change the existing translatePageAction dispatch — the content script reads includeImages from storage at translate time (Plan 04).
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (no `any`; HTMLInputElement typed; Settings.includeImages used).
    - `grep -q 'id="includeImages"' src/popup.html` succeeds.
    - `grep -q 'includeImages' src/popup.ts` succeeds AND the change handler spreads existing settings (`grep -q '\.\.\.' src/popup.ts` near the himeSettings write — manual confirm the merge preserves other fields, NOT a bare overwrite).
    - `grep -q '?? false' src/popup.ts` (or equivalent default-OFF) confirms unset → unchecked.
  </acceptance_criteria>
  <done>The popup renders an Include images checkbox under Translate page, reflects the stored includeImages on open (default OFF), and persists toggles into himeSettings without clobbering other settings fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popup → chrome.storage.local | The checkbox value is a non-secret user preference; no key material involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | himeSettings merge-write | mitigate | The change handler reads-then-merges (spread existing fields) so toggling includeImages cannot drop the API keys / provider / language settings stored in the same himeSettings object. |
| T-16-08 | Information Disclosure | checkbox state | accept | includeImages is a boolean preference, not a secret; storing it in local storage is the established settings pattern (no PII, no key). |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (milestone guardrail: zero new OSS deps). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm test` unaffected (no logic touched outside popup).
- Merge-write confirmed (other himeSettings fields preserved on toggle).
</verification>

<success_criteria>
- The opt-in "Include images" checkbox exists with the Translate page control, defaults OFF, and round-trips through himeSettings — the D-01 gate the content overlay pass reads (OVL-01).
</success_criteria>

<output>
Create `.planning/phases/16-in-place-image-overlay-translation/16-03-SUMMARY.md` when done.
</output>
