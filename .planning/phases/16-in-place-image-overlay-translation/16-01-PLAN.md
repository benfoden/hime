---
phase: 16-in-place-image-overlay-translation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/overlay-geometry.ts
  - src/overlay-fit.ts
  - src/image-resolve.ts
  - src/types.ts
  - test/overlay-geometry.mjs
  - test/overlay-fit.mjs
  - test/image-resolve.mjs
  - test/fixtures/fulltextannotation.json
autonomous: true
requirements: [OVL-01, OVL-04, OVL-05]
must_haves:
  truths:
    - "mapBox converts a submitted-pixel vertex box to a rendered-rect rect under downscale + object-fit contain/cover/fill"
    - "fitText returns the largest font that fits and reports clamped=true when even the floor overflows"
    - "collectParagraphBoxes assembles paragraph text + a 4-vertex box per paragraph from a Vision fullTextAnnotation"
  artifacts:
    - path: "src/overlay-geometry.ts"
      provides: "Pure mapBox() three-transform coordinate mapping"
      exports: ["mapBox"]
      min_lines: 30
    - path: "src/overlay-fit.ts"
      provides: "Pure fitText() binary-search shrink-to-fit"
      exports: ["fitText"]
      min_lines: 25
    - path: "src/image-resolve.ts"
      provides: "collectParagraphBoxes() paragraph geometry extraction"
      contains: "export function collectParagraphBoxes"
    - path: "src/types.ts"
      provides: "OverlayBlock + TranslateImageBlocks message/response + includeImages setting"
      contains: "OverlayBlock"
    - path: "test/overlay-geometry.mjs"
      provides: "mapBox node tests (downscale, object-fit, scroll)"
    - path: "test/overlay-fit.mjs"
      provides: "fitText node tests (fits, shrinks-to-floor, clamps, CJK)"
  key_links:
    - from: "test/overlay-geometry.mjs"
      to: "dist/overlay-geometry.js"
      via: "dynamic import of compiled module"
      pattern: "dist/overlay-geometry"
    - from: "src/overlay-fit.ts"
      to: "measure callback"
      via: "injected (text, fontPx) => widthPx"
      pattern: "measure"
---

<objective>
Create the two new pure, node-testable math seams this phase hangs on — `src/overlay-geometry.ts` (`mapBox()`) and `src/overlay-fit.ts` (`fitText()`) — extend `src/image-resolve.ts` with `collectParagraphBoxes()` to surface per-paragraph geometry from the Vision `fullTextAnnotation`, and define every new TypeScript contract in `src/types.ts` (the `OverlayBlock` shape, the `translateImageBlocks` message + response, and the `includeImages` setting). All math lands behind RED-first node tests before any content.ts/background.ts wiring exists.

Purpose: The single dominant risk of this phase is coordinate-space mismatch (RESEARCH Pitfall 1). Isolating `mapBox`/`fitText` as pure functions with stubbed inputs lets us prove the three-transform math and shrink-to-fit math in node — the only layer that CAN be automatically tested (content.ts canvas/DOM is human-verify only). Per OVL-01/04/05.
Output: Two new pure modules + their node tests, the extended `collectParagraphBoxes` walk + its test, a fixture `fullTextAnnotation` JSON, and the type contracts every downstream plan imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-in-place-image-overlay-translation/16-RESEARCH.md
@.planning/phases/16-in-place-image-overlay-translation/16-PATTERNS.md
</context>

<artifacts_this_phase_produces>
The complete inventory of NEW symbols/files/fields introduced across all 5 phase-16 plans (downstream executors implement against these exact names):

**New files (Plan 01):**
- `src/overlay-geometry.ts` — pure module. Exports: `mapBox(box, submitted, natural, rendered, objectFit)`, `interface Vertex`, `type ObjectFit`. Private `objectFitTransform()`.
- `src/overlay-fit.ts` — pure module. Exports: `fitText(text, boxW, boxH, measure, opts?)`. Private `wrapLines()`.
- `test/overlay-geometry.mjs`, `test/overlay-fit.mjs` — node:test files (against dist/).
- `test/fixtures/fulltextannotation.json` — realistic Vision fullTextAnnotation fixture.

**New symbols in existing files (Plan 01):**
- `src/image-resolve.ts`: `export interface OverlayBlock { text: string; box: { x:number; y:number }[] }`, `export function collectParagraphBoxes(fta, submitted?)`, private `assembleParagraphText(para)`; raw interfaces widened with optional `boundingBox` (vertices/normalizedVertices) + symbol `text`/`property.detectedBreak`.
- `src/types.ts`: `'translateImageBlocks'` added to `MessageType` union; `interface TranslateImageBlocksMessage` (`payload:{srcUrl,tabId?,dedupKey,config}` — NO key); `interface TranslateImageBlocksResponse` (`blocks?:[{box,original,translated}], submitted?:{w,h}, captureFallback?, error?, kind?`); `OcrResult.blocks?: OverlayBlock[]`; `Settings.includeImages?: boolean`; `DEFAULT_SETTINGS.includeImages: false`.

**Widened worker return (Plan 02):**
- `src/background.ts`: `downscaleAndGuard` return widened `{base64,mime}` → `{base64,mime,width,height}` (submitted post-downscale dims — the field `mapBox` needs); `resolveImageBytes` gains a `viaCaptureFallback` signal; new `case 'translateImageBlocks'` in the onMessage switch.
- `src/providers/vision-google.ts`: `ocr()` return gains `blocks: OverlayBlock[]` (via `collectParagraphBoxes`); raw `VisionParagraph`/`VisionWord` interfaces widened with `boundingBox`/`symbols.text`/`detectedBreak`.

**Popup (Plan 03):**
- `src/popup.html`: `<input type="checkbox" id="includeImages">` ("Include images") under the Translate page control.
- `src/popup.ts`: `includeImagesCheckbox` ref + load/persist wiring into `himeSettings.includeImages`.

**Content overlay layer (Plan 04):**
- `src/content.ts`: verbatim MIRRORS of `mapBox`/`fitText` (with the classic-script in-sync comment); `overlayTranslateImages()` (includeImages-gated collect+dispatch), `overlayRenderImage(img, reply)` (container + mapBox-placed boxes + fitText), `overlayApplyGlobal(show)`, `overlayRemoveAll()`, per-image corner toggle button; reposition extended for overlay containers (scroll/resize + ResizeObserver).

**Storage key:** `himeSettings.includeImages` (chrome.storage.local), default `false`.
**Message types:** `translateImageBlocks` (content → worker) + its response.
**Fixed overlay palette (D-03):** `background:rgba(0,0,0,0.78)`, `color:#fff`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: overlay-geometry.ts mapBox() + RED node test</name>
  <read_first>
    - src/image-resolve.ts:1-6 (the "Pattern law" module header — NO chrome.*, NO document.*; copy an analogous header verbatim) and :60-76 (targetDimensions / downscaleTarget — the downscale source the ratio undoes)
    - test/image-resolve.mjs (node:test + node:assert/strict, imports from ../dist/, "verify against dist/, NEVER the service-worker console" — the test shape to mirror)
    - .planning/phases/16-in-place-image-overlay-translation/16-RESEARCH.md §"Pattern 3" (the three composable transforms + object-fit letterbox math)
  </read_first>
  <behavior>
    - mapBox(box, submitted{w,h}, natural{w,h}, rendered{w,h}, objectFit) returns {left,top,width,height} relative to the RENDERED image rect (viewport offset is the container's job, NOT mapBox).
    - Transform (a) undo downscale: upX=natural.w/submitted.w, upY=natural.h/submitted.h; box vertices scaled to natural-pixel space; axis-aligned bbox via Math.min/Math.max over the 4 vertices (ignore skew).
    - Transform (b) object-fit letterbox: 'fill' → independent scaleX=rendered.w/natural.w, scaleY=rendered.h/natural.h, zero offset; 'contain' → scale=min(rW/nW,rH/nH), centered offX=(rW-nW*scale)/2, offY=(rH-nH*scale)/2; 'cover' → scale=max(...), negative offsets; 'none' → scale=1 centered; 'scale-down' → behaves as the smaller of none/contain.
    - Test: a 2048-wide submitted box on a 4096-wide natural image rendered at 1024 → x positions land at quarter scale (downscale undone THEN rendered-scale applied), object-fit:fill.
    - Test: object-fit:contain on a non-matching aspect ratio centers the box with a positive letterbox offset.
    - Test: object-fit:cover yields negative offset (image overflows).
    - Test: degenerate submitted dim 0 must NOT divide-by-zero into NaN — guard to a zero/identity result.
  </behavior>
  <action>
    Create `src/overlay-geometry.ts` as a PURE module (Pattern law header copied from image-resolve.ts:1-6, adapted). Export `interface Vertex { x: number; y: number }` and `type ObjectFit = 'fill' | 'contain' | 'cover' | 'none' | 'scale-down'`. Export `function mapBox(box: Vertex[], submitted: {w:number;h:number}, natural: {w:number;h:number}, rendered: {w:number;h:number}, objectFit: ObjectFit): {left:number; top:number; width:number; height:number}` implementing the three transforms from RESEARCH Pattern 3. Add a private `objectFitTransform(natural, rendered, objectFit)` helper returning `{scale|scaleX/scaleY, offX, offY}`. Guard submitted.w/h <= 0 (return a safe zero rect — do not emit NaN). NO chrome.*, NO document.*, NO canvas. Create `test/overlay-geometry.mjs` mirroring test/image-resolve.mjs (node:test, node:assert/strict, dynamic import of `../dist/overlay-geometry.js`) with the behavior cases above — written RED-first (assertions encode the expected numbers derived by hand from the transform formulas, not from the implementation).
  </action>
  <verify>
    <automated>npm run build && node --test test/overlay-geometry.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/overlay-geometry.mjs` exits 0 with all cases passing.
    - `grep -c 'document\.\|chrome\.' src/overlay-geometry.ts` returns 0 (Pattern law: no DOM/chrome refs).
    - `grep -q 'export function mapBox' src/overlay-geometry.ts` succeeds.
    - The downscale-undo case asserts an exact numeric left/width derived from natural/submitted ratio × rendered scale (no off-by-downscale-factor drift).
  </acceptance_criteria>
  <done>mapBox composes downscale-undo → object-fit letterbox correctly for fill/contain/cover/none, guards zero dims, and is fully node-tested with no DOM/chrome references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: overlay-fit.ts fitText() + RED node test</name>
  <read_first>
    - src/image-resolve.ts:1-6 (Pattern law header) and :96 (isCjkLang — exported; reuse for CJK per-character wrap decision)
    - src/page-walk.ts:51-86 (pure module with injected-callback / plain-input shape — the seam style to mirror)
    - .planning/phases/16-in-place-image-overlay-translation/16-RESEARCH.md §"Pattern 6" (binary-search font + greedy wrap + floor → clamp; CJK per-char wrap)
  </read_first>
  <behavior>
    - fitText(text, boxW, boxH, measure, opts) returns {fontPx, clamped, lines}; measure: (text, fontPx) => widthPx is INJECTED (node stubs it — no canvas).
    - Binary-search fontPx in [opts.minFont, opts.maxFont]: for each candidate font, greedily wrap into lines whose measured width ≤ boxW-2*pad; accept the font if totalLines*fontPx*lineHeight ≤ boxH-2*pad. Largest fitting font wins.
    - Wrap granularity: per-word (split on whitespace) for non-CJK; per-character when isCjkLang(detectedLang) — CJK glyphs do not break on spaces.
    - Test (fits): short text in a generous box → fontPx == maxFont, clamped == false.
    - Test (shrinks): long text in a small box → fontPx strictly between minFont and maxFont, clamped == false.
    - Test (clamps): text that overflows even at minFont → fontPx == minFont, clamped == true.
    - Test (CJK): a CJK string with no spaces wraps per-character (lines length reflects char-based wrap, not one giant unbreakable line), using a stubbed measure of ~1em/char.
  </behavior>
  <action>
    Create `src/overlay-fit.ts` as a PURE module (Pattern law header). Export `function fitText(text: string, boxW: number, boxH: number, measure: (text: string, fontPx: number) => number, opts?: { maxFont?: number; minFont?: number; lineHeight?: number; pad?: number; cjk?: boolean }): { fontPx: number; clamped: boolean; lines: string[] }` with defaults `maxFont:28, minFont:9, lineHeight:1.2, pad:4`. Internal `wrapLines(text, fontPx, maxWidth, measure, cjk)` does greedy word- or char-wrap. Binary-search the font; if even minFont overflows the height, return `{ fontPx: minFont, clamped: true, lines }`. Pass the `cjk` decision in via opts (caller computes `isCjkLang(detectedLang)`) so this module stays free of language tables. NO chrome.*, NO document.*, NO canvas. Create `test/overlay-fit.mjs` mirroring test/image-resolve.mjs with a deterministic stubbed `measure` (e.g. width = chars × fontPx × widthFactor; CJK factor ~1.0, latin ~0.5) and the four behavior cases above — RED-first.
  </action>
  <verify>
    <automated>npm run build && node --test test/overlay-fit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/overlay-fit.mjs` exits 0 with all four cases passing.
    - `grep -c 'document\.\|chrome\.\|measureText' src/overlay-fit.ts` returns 0 (measure is injected, never called natively here).
    - `grep -q 'export function fitText' src/overlay-fit.ts` succeeds.
    - The clamp case asserts `clamped === true` AND `fontPx === 9` (minFont floor honored, never below).
  </acceptance_criteria>
  <done>fitText binary-searches the largest fitting font, wraps per-word or per-char, floors at minFont with clamped=true, and is node-tested against a stubbed measure with no canvas dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: collectParagraphBoxes + OverlayBlock/message/setting contracts + fixture + RED test</name>
  <read_first>
    - src/image-resolve.ts:155-215 (the FullTextAnnotation/PageNode/BlockNode/ParagraphNode/WordNode local interfaces + the meanWordConfidence pages→blocks→paragraphs→words→symbols walk — the EXACT nesting to extend; note there is NO paragraph.text — Pitfall 3)
    - src/types.ts:78-87 (OcrResult — extend the worker-facing OCR shape), :135-161 (MessageType union — add 'translateImageBlocks'), :350-364 (TranslatePageBatchMessage + TranslatePageBatchResponse — the message/response analog to clone), :452-480 (DEFAULT_SETTINGS + migrateSettings — add includeImages, migrate-safe)
    - .planning/phases/16-in-place-image-overlay-translation/16-RESEARCH.md §"Pattern 1" (paragraph-level recommendation, assemble text from words[].symbols[].text + detectedBreak, vertices vs normalizedVertices fallback A2)
  </read_first>
  <behavior>
    - collectParagraphBoxes(fta) returns OverlayBlock[] — one entry per paragraph with non-empty assembled text AND a 4-vertex box; paragraphs with empty text or a non-4-vertex box are dropped.
    - Paragraph text is assembled from words[].symbols[].text joined with break spacing from symbol.property.detectedBreak.type (SPACE / EOL_SURE_SPACE → ' ', LINE_BREAK → '\n'); there is no paragraph.text field (Pitfall 3).
    - box reads boundingBox.vertices when present; falls back to boundingBox.normalizedVertices scaled by submitted dims when vertices absent (A2). (Pass submitted dims into the fn for the normalized fallback, or document that normalized fallback multiplies by submitted dims supplied by the caller.)
    - Test: a fixture fullTextAnnotation with 2 paragraphs → 2 OverlayBlocks with assembled text and 4-vertex boxes.
    - Test: a paragraph with whitespace-only symbols is dropped (no empty boxes).
  </behavior>
  <action>
    In `src/image-resolve.ts`, extend the local raw interfaces (`ParagraphNode`, `WordNode`/symbol shape) to carry optional `boundingBox?: { vertices?: {x?:number;y?:number}[]; normalizedVertices?: {x?:number;y?:number}[] }` and symbol `{ text?: string; property?: { detectedBreak?: { type?: string } } }`. Add and export `interface OverlayBlock { text: string; box: { x: number; y: number }[] }` and `function collectParagraphBoxes(fta: FullTextAnnotation | undefined | null, submitted?: { w: number; h: number }): OverlayBlock[]` reusing the meanWordConfidence nesting (pages→blocks→paragraphs), with a private `assembleParagraphText(para)` joining symbols with detectedBreak spacing. Keep optional-chaining-everywhere defensiveness. In `src/types.ts`: add `'translateImageBlocks'` to the `MessageType` union; add `interface TranslateImageBlocksMessage extends Message` (`payload: { srcUrl: string; tabId?: number; dedupKey: string; config: TranslationConfig }` — NO key, T-12-01) and `interface TranslateImageBlocksResponse { blocks?: { box: {x:number;y:number}[]; original: string; translated: string }[]; submitted?: { w: number; h: number }; captureFallback?: boolean; error?: string; kind?: import('./errors.js').ErrorKind }`; add `includeImages?: boolean` to the `Settings` interface and `includeImages: false` to `DEFAULT_SETTINGS` (migrateSettings spreads over defaults → missing field defaults to false, no migration). Re-export `OverlayBlock` from types if helpful, or have content/background import from image-resolve. Create `test/fixtures/fulltextannotation.json` (a realistic pages→blocks→paragraphs→words→symbols structure with boundingBox.vertices and detectedBreak), and EXTEND `test/image-resolve.mjs` with the two behavior cases (import the fixture, assert collectParagraphBoxes output) — RED-first.
  </action>
  <verify>
    <automated>npm run build && node --test test/image-resolve.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/image-resolve.mjs` exits 0 including the new collectParagraphBoxes cases.
    - `grep -q 'export function collectParagraphBoxes' src/image-resolve.ts` succeeds.
    - `grep -q "'translateImageBlocks'" src/types.ts` succeeds AND `grep -q 'includeImages' src/types.ts` succeeds (default false present in DEFAULT_SETTINGS).
    - `npx tsc --noEmit` passes (no `any`; new contracts typecheck).
    - The 2-paragraph fixture case asserts exactly 2 OverlayBlocks each with a `box.length === 4`.
  </acceptance_criteria>
  <done>collectParagraphBoxes assembles paragraph text + 4-vertex boxes (vertices with normalized fallback), the OverlayBlock/translateImageBlocks contracts + includeImages setting typecheck, and the fixture-backed extraction test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vision provider JSON → fullTextAnnotation parse | Provider response is untrusted structured input; parsed defensively (optional chaining, length/shape guards). |
| LLM reply (later plans) → keyed-JSON parse | Out of scope for this plan, but the message/response contract defined here carries NO key and only typed geometry/text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | collectParagraphBoxes parsing a malformed Vision response | mitigate | Optional-chaining everywhere; drop paragraphs with empty text or non-4-vertex boxes; never throw on a sparse annotation (mirrors meanWordConfidence defensiveness, image-resolve.ts:183). |
| T-16-02 | Information Disclosure | TranslateImageBlocksMessage payload shape | mitigate | Contract carries `{srcUrl, tabId, dedupKey, config}` ONLY — no apiKey field exists in the type, so a key cannot be serialized into the message (T-12-01 enforced at the type level). |
| T-16-03 | Denial of Service | fitText binary search / mapBox zero-dim | mitigate | Bounded binary search (minFont..maxFont integer steps); mapBox guards submitted.w/h ≤ 0 to a safe rect (no NaN/Infinity propagation). |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (milestone guardrail: zero new OSS deps); nothing to slopcheck. |
</threat_model>

<verification>
- `npm test` green (full suite: tsc + all node tests, including the three new/extended test files).
- New pure modules contain no `document.`/`chrome.` references (Pattern law).
- `npx tsc --noEmit` clean — no `any`, all new contracts typecheck.
</verification>

<success_criteria>
- mapBox proven under downscale + object-fit contain/cover/fill + zero-dim guard (OVL-04 math).
- fitText proven for fits/shrinks/clamps/CJK (OVL-05 math).
- collectParagraphBoxes proven against a fixture fullTextAnnotation (OVL-01 geometry source).
- OverlayBlock, translateImageBlocks message/response, and includeImages setting all defined and typechecking — the contracts every downstream plan consumes.
</success_criteria>

<output>
Create `.planning/phases/16-in-place-image-overlay-translation/16-01-SUMMARY.md` when done.
</output>
