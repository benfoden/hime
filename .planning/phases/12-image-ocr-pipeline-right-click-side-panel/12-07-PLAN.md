---
phase: 12-image-ocr-pipeline-right-click-side-panel
plan: 07
type: execute
wave: 5
depends_on: ["12-05", "12-06"]
files_modified:
  - test/vision-live.mjs
autonomous: false
requirements: [IMG-01, IMG-04, IMG-07, VIS-01, VIS-03]
must_haves:
  truths:
    - "With a real Google Cloud key (Vision + Translation enabled), a node harness against dist/providers/vision-google.js OCRs + translates a real image end-to-end"
    - "Right-clicking a same-origin image opens the panel within the gesture and shows the result"
    - "A cross-origin / CDN image resolves via the fetch-or-capture ladder (no tainted-canvas error)"
    - "A text-free image renders the explicit 'no text found' state (not a blank)"
    - "The DevTools network panel shows the Google calls originate from the service worker, never the page; the key is not visible in any page-context request"
  artifacts:
    - path: "test/vision-live.mjs"
      provides: "opt-in live provider smoke test (skips without GOOGLE_API_KEY) — verifies VIS-01 against dist/ with a real key per the test law"
  key_links:
    - from: "test/vision-live.mjs"
      to: "dist/providers/vision-google.js"
      via: "real ocrTranslate call gated on env key"
      pattern: "ocrTranslate"
---

<objective>
Close the phase with the verifications that genuinely cannot be node-mocked: (1) an opt-in LIVE provider smoke test that drives `dist/providers/vision-google.js` with a real Google Cloud key against a real image (per the test law — node harness on dist/, env-supplied key, NEVER the service-worker console), and (2) a human-verify checkpoint for the in-browser behaviors — the sidePanel gesture, the cross-origin capture fallback, the no-text state, and the worker-side key invariant observed in DevTools.

Purpose: Phases 12 success criteria #1, #3, #4, #5 hinge on real-browser behavior (gesture timing, cross-origin fetch/capture, MV3 lifecycle, key-in-worker) that the mocked unit tests in Plans 02-06 cannot prove. This plan is the phase gate before `/gsd-verify-work`.

Output: `test/vision-live.mjs` (automated, opt-in) and a recorded human-verify result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-RESEARCH.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-05-SUMMARY.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `test/vision-live.mjs` — opt-in node test: skips (passes as no-op) when `GOOGLE_API_KEY` is unset; when set, base64-encodes a small bundled/fixture image containing text and calls `GoogleVisionProvider.ocrTranslate` against the live endpoints, asserting a non-empty originalText + translatedText + detectedLang. Never logs the key.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Live provider smoke test (opt-in, node harness on dist/)</name>
  <files>test/vision-live.mjs</files>
  <read_first>
    - test/serp.mjs (L1-60 — import-from-dist conventions)
    - src/providers/vision-google.ts (ocrTranslate signature — Plan 02)
    - MEMORY: no-service-worker-console-tests — verify hime via node harness on dist/, never the SW console
    - .planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-RESEARCH.md §Environment Availability (Google key via env to a node harness importing dist/) + §Open Question 2 (both APIs must be enabled on the key's project)
  </read_first>
  <action>
    Create `test/vision-live.mjs` importing `GoogleVisionProvider` from `dist/providers/vision-google.js`. At the top, read `process.env.GOOGLE_API_KEY`; if unset, register a single `test(..., { skip: true })` (or log "skipped: no GOOGLE_API_KEY" and return) so the default `npm test` stays green offline. When the key IS present: build a base64 of a tiny image fixture that contains legible text (a small bundled PNG under test/fixtures/, or generate one), call `provider.ocrTranslate(base64, 'image/png', 'English', key)`, and assert `originalText` is non-empty, `translatedText` is non-empty, and `detectedLang` is a plausible code. Wrap in a try/finally that NEVER logs the key (assert-style: do not interpolate `key` into any console output). Document the run oneliner in the test header: a single-line `GOOGLE_API_KEY=... node --test test/vision-live.mjs` (the key's project must have BOTH Cloud Vision API and Cloud Translation API enabled — Open Question 2).
  </action>
  <verify>
    <automated>npm run build && node --test test/vision-live.mjs 2>&1 | grep -Eq 'skip|pass|tests [0-9]'</automated>
  </verify>
  <done>test/vision-live.mjs runs and SKIPS cleanly without GOOGLE_API_KEY (default suite stays green); when GOOGLE_API_KEY is set it exercises the live two-call provider against dist/ and asserts a real OCR+translation, never logging the key.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-browser end-to-end smoke (load-unpacked)</name>
  <what-built>
    The full manual image-translation slice is wired: a right-click context menu on images, a side panel that opens synchronously inside the gesture, the worker-side fetch→capture byte ladder + OffscreenCanvas downscale, the two-call Google provider, durable storage.session job state, and the prepend/textContent panel renderer. Plans 02-06's unit suites are green; Task 1's live provider smoke (if a key was supplied) passed. These browser behaviors cannot be node-tested.
  </what-built>
  <how-to-verify>
    Preconditions: `npm run build`; in `chrome://extensions` (Developer mode) Load unpacked → `dist/`. Seed the BYOK key for Phase 12 (the settings field is Phase 14): write `googleApiKey` into the extension's `chrome.storage.local` himeSettings (e.g. via the extension's service-worker storage, or a temporary options write) — the key's Google Cloud project must have BOTH Cloud Vision API and Cloud Translation API enabled. Confirm the global target language is set (e.g. English) in options.

    1. Same-origin / fetchable image (SC#1, SC#2): Open a page with a clear text-bearing image (e.g. a Japanese sign/poster). Right-click it → confirm "Translate image with hime" appears → click it. EXPECT: the side panel opens immediately (within the click — no "user gesture" error in the SW console), shows a loading skeleton, then fills with a thumbnail, "Detected: X → Y", the translation (above), and the original text (below) with line breaks preserved.
    2. Cross-origin / CDN image (SC#3, IMG-04): Repeat on a hotlink-protected or cross-origin CDN image (or a CSS-tainted one). EXPECT: a result still appears (via the fetch-or-captureVisibleTab fallback) — NOT a "Tainted canvas" SecurityError and NOT a blank entry. Each new right-click PREPENDS above the previous (D-01).
    3. Text-free image (SC#3, IMG-05): Right-click a photo/logo with no readable text. EXPECT: the explicit "No text found" state (no silent blank, no garbage translation).
    4. Key-in-worker invariant (SC#4, IMG-07): Open the PAGE's DevTools Network tab (not the SW's). EXPECT: NO request to vision.googleapis.com / translation.googleapis.com originates from the page context, and the googleApiKey string does not appear in any page-context request/header. The Google calls appear only under the service worker's inspect view.
    5. Worker-restart durability (SC#5): With a result showing, stop the service worker (chrome://extensions → service worker → terminate), then reopen/refresh the side panel. EXPECT: the prior result(s) repopulate from storage.session — the panel does not blank. Trigger a new translation and confirm it still completes or shows an explicit error within ~25s (no indefinite "translating…").
    6. Error path: Temporarily clear/break the key and right-click an image. EXPECT: an explicit per-entry error row (auth kind) — never a blank.
  </how-to-verify>
  <resume-signal>Type "approved" if all six behaviors hold, or describe which step failed (and any SW/page console error) so the relevant plan can be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live key → Google (test) | the live smoke uses a real BYOK key via env; must not be logged or committed |
| browser page context → Google | the human check confirms no page-context call/key leak (IMG-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19 | Information disclosure | live key in test/CI logs | mitigate | `test/vision-live.mjs` reads the key from env only, skips without it, and never interpolates it into output; the default `npm test` does not require or expose it. |
| T-12-20 | Information disclosure | key reaching the page (verified) | mitigate | Checkpoint step 4 explicitly inspects the PAGE network context to confirm the key/Google calls never originate there (IMG-07 acceptance). |
| T-12-21 | Tampering / silent failure (verified) | cross-origin + no-text + restart paths | mitigate | Checkpoint steps 2/3/5 confirm the fallback ladder, the explicit no-text state, and storage.session durability behave in a real browser (never blank). |
</threat_model>

<verification>
- `npm test` stays green offline (vision-live.mjs skips without the key).
- With GOOGLE_API_KEY set, the live provider smoke passes against dist/.
- The six in-browser behaviors are human-confirmed (gesture open, cross-origin fallback, no-text, key-in-worker via DevTools, restart durability, error path).
</verification>

<success_criteria>
- Phase 12 SC#1-#5 confirmed in a real browser; VIS-01 confirmed live against dist/.
- No page-context Google call or key exposure (IMG-07).
- All automated suites green; manual checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-07-SUMMARY.md` when done.
</output>
