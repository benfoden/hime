---
phase: 12-image-ocr-pipeline-right-click-side-panel
plan: 04
type: tdd
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/panel-render.ts
  - test/panel-render.mjs
autonomous: true
requirements: [IMG-02, IMG-03, IMG-05]
must_haves:
  truths:
    - "A populated entry renders thumbnail + 'Detected: X → Y' line + translation (above) + original (below), in that order (D-02)"
    - "OCR line/paragraph breaks survive visually (text nodes carry a pre-wrap class)"
    - "A low-confidence entry shows an amber badge; a no-text entry shows the explicit no-text state; an error entry shows a per-entry error row — never a silent blank (IMG-05)"
    - "A new result PREPENDS to the session list (newest on top, D-01) — not replaceChildren"
    - "All OCR/translation text is rendered via textContent only; an XSS probe string injects no element"
  artifacts:
    - path: "src/panel-render.ts"
      provides: "renderPanel(state, doc, mount), prependEntry(entry, doc, mount), entryEl(doc, entry) — DOM-agnostic, textContent-only, no global document/window"
      exports: ["renderPanel", "prependEntry"]
      contains: "textContent"
    - path: "test/panel-render.mjs"
      provides: "linkedom tests for all states + prepend order + breaks + XSS"
  key_links:
    - from: "src/sidepanel.ts (plan 06)"
      to: "src/panel-render.ts"
      via: "import { renderPanel, prependEntry }"
      pattern: "renderPanel|prependEntry"
    - from: "src/panel-render.ts"
      to: "src/types.ts"
      via: "import type { ImageState, ImageEntry, ImageResult }"
      pattern: "ImageState"
---

<objective>
Build `src/panel-render.ts` — a structural clone of `serp-render.ts` that renders the side panel's image-result session list: per-entry states (loading, populated, no-text, low-confidence amber badge, error) with thumbnail + "Detected: X → Y" + translation-above-original layout (D-02), breaks preserved, and D-01 prepend. TDD: complete Plan 01's `test/panel-render.mjs` scaffold (RED), then implement (GREEN) using linkedom exactly as `serp-render.ts` is tested.

Purpose: IMG-02/03/05's render surface. The XSS guarantee (textContent only) and the DOM-agnostic contract (Document injected, never imported by the node harness) are copied verbatim from `serp-render.ts`. The one deliberate divergence is D-01 prepend instead of `replaceChildren`.

Output: `src/panel-render.ts` and a green `test/panel-render.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-RESEARCH.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-PATTERNS.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-01-SUMMARY.md
@src/serp-render.ts
</context>

<artifacts_this_phase_produces>
- `renderPanel(state: ImageState, doc: Document, mount: HTMLElement): void` — full render (used on panel open / rebuild from storage.session); for `kind:'list'` it renders entries NEWEST-FIRST (D-01), for `kind:'empty'` an empty state
- `prependEntry(entry: ImageEntry, doc: Document, mount: HTMLElement): void` — adds/updates a single entry at the TOP (D-01) without wiping the list; used on each incoming worker push and to swap a skeleton for its result
- `entryEl(doc, entry): HTMLElement` (internal) — builds one entry node via the textContent-only `el()` helper (copied from serp-render)
- CSS class names used (styled in Plan 06's sidepanel.css): `.hime-entry`, `.hime-entry-thumb`, `.hime-entry-direction`, `.hime-entry-translation`, `.hime-entry-original`, `.hime-entry-prewrap` (white-space:pre-wrap, D-02), `.hime-badge-amber` (D-04), `.hime-entry-error`, `.hime-entry-loading`
</artifacts_this_phase_produces>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Complete the linkedom panel-render tests</name>
  <files>test/panel-render.mjs</files>
  <read_first>
    - test/panel-render.mjs (the Plan 01 scaffold)
    - test/serp.mjs (L1-60 — parseHTML, fresh-mount-per-test helper, XSS_PROBE usage)
    - src/panel-mock.ts (fixtures incl. XSS_PROBE)
    - src/types.ts (ImageState, ImageEntry — from Plan 01)
  </read_first>
  <action>
    Flesh out `test/panel-render.mjs` (import `renderPanel`, `prependEntry` from `dist/panel-render.js`; `parseHTML` from linkedom; fixtures from `dist/panel-mock.js`). Cases:
    1. populated entry: `renderPanel({kind:'list', entries:[populated]}, doc, mount)` produces an entry containing an `<img>` thumbnail, a direction line whose text matches `Detected:` and an arrow, a translation node, and an original node; assert DOM order is thumbnail → direction → translation → original (D-02, translation ABOVE original).
    2. breaks preserved: the translation/original text nodes carry the pre-wrap class (`.hime-entry-prewrap`); the multi-line fixture text is set via textContent and the class is present.
    3. low-confidence: a populated entry with `lowConfidence:true` renders the `.hime-badge-amber` element; with `lowConfidence:false` it does NOT.
    4. no-text: a `{kind:'no-text'}` entry renders an explicit no-text message node (not blank).
    5. error: a `{kind:'error', errorKind:'auth', message}` entry renders an `.hime-entry-error` row with the message text.
    6. D-01 prepend: start with one entry in the mount, call `prependEntry(newEntry, ...)`; assert the NEW entry is `mount.firstElementChild` and the old entry is still present (count = 2). Assert `renderPanel` on a list renders entries newest-first.
    7. XSS: an entry whose result.translatedText === XSS_PROBE renders the probe as text — `mount.querySelector('img[src="x"]')` is null and the probe string appears as textContent.
  </action>
  <verify>
    <automated>npm run build && node --test test/panel-render.mjs 2>&1 | grep -Eq 'fail [1-9]|tests [1-9]'</automated>
  </verify>
  <done>test/panel-render.mjs encodes all seven cases and is RED until the subject lands.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement panel-render.ts (clone of serp-render.ts)</name>
  <files>src/panel-render.ts</files>
  <read_first>
    - src/serp-render.ts (full — header doctrine L1-11; SerpState union L17-21; renderPanel/exhaustiveness L37-58; el() helper L64-74; faviconEl img precedent L91-108; el(doc,'p',{text,className}) snippet L129)
    - src/types.ts (ImageState, ImageEntry, ImageResult — from Plan 01)
    - .planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-PATTERNS.md §"src/panel-render.ts" (every divergence: prepend not replaceChildren; pre-wrap class; entry order; low-conf = populated+badge)
  </read_first>
  <action>
    Implement `src/panel-render.ts` to pass Task 1. Copy the serp-render.ts header doctrine verbatim (references NO global document/window; Document injected; XSS guarantee = textContent only, NEVER innerHTML, no sanitizer). Copy the `el(doc, tag, opts)` helper (L64-74) verbatim — it is the actual XSS guard (`node.textContent = opts.text`).

    Import `type { ImageState, ImageEntry, ImageResult }` from `./types.js`.
    - `renderPanel(state, doc, mount)`: switch on `state.kind` with a `const _never: never = state` exhaustiveness default (clone L37-58). For `'empty'` render an empty-state node. For `'list'` render `state.entries` NEWEST-FIRST into the mount (the entries array is already newest-first; use replaceChildren-then-append OR build a fragment — full render is fine here, but a SINGLE new result must go through `prependEntry`, NOT renderPanel).
    - `prependEntry(entry, doc, mount)`: build the entry node and `mount.prepend(node)` (D-01). If an entry with the same `id` already exists (a skeleton being filled), replace that node in place instead of duplicating.
    - `entryEl(doc, entry)`: switch on `entry.kind`:
      - `loading` → a `.hime-entry .hime-entry-loading` node (skeleton).
      - `populated` → `.hime-entry` containing, in order: thumbnail `<img>` (`.hime-entry-thumb`, set `img.src = entry.thumbnailUrl`, `alt=''` — faviconEl precedent L91-108), a `.hime-entry-direction` line "Detected: {detectedLang} → {target}" (the worker/panel supplies the resolved display names; render the provided string verbatim via textContent), a `.hime-entry-translation .hime-entry-prewrap` node with `entry.result.translatedText`, then a `.hime-entry-original .hime-entry-prewrap` node with `entry.result.originalText`. If `entry.lowConfidence` append a `.hime-badge-amber` node ("low confidence"). (D-02: translation above original; pre-wrap preserves breaks.)
      - `no-text` → `.hime-entry` with an explicit "No text found" message node (IMG-05).
      - `error` → `.hime-entry .hime-entry-error` with the message text (IMG-05; never blank).
    Every text string set via `el()`/textContent; thumbnail via `img.src`. No innerHTML anywhere. No fenced code in the PLAN.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5; npm run build && node --test test/panel-render.mjs</automated>
  </verify>
  <done>test/panel-render.mjs GREEN; all five entry states render explicitly; prepend keeps prior entries; pre-wrap class present on text; XSS probe renders as text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImageResult text → panel DOM | OCR + translation strings (provider-sourced, attacker-influenceable via image content) are rendered into a chrome-extension:// page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering (XSS) | panel-render text + thumbnail | mitigate | `el()` sets `textContent` only, never `innerHTML`; thumbnail via `img.src`/`alt=''` (serp-render precedent). Task 1 case 7 asserts XSS_PROBE injects no element. |
| T-12-10 | Spoofing (silent failure) | per-entry states | mitigate | Every entry kind (loading/populated/no-text/low-confidence/error) renders an explicit node; exhaustiveness `never` default makes a missing state a compile error → never a silent blank (IMG-05). |
</threat_model>

<verification>
- `npm test` green including panel-render.mjs.
- All states render explicitly; D-01 prepend; D-02 breaks; textContent-only proven by the XSS case.
</verification>

<success_criteria>
- IMG-02: thumbnail + translation + original shown.
- IMG-03: "Detected: X → Y" direction line rendered.
- IMG-05/D-04: no-text, low-confidence (amber), error states explicit; never blank.
- D-01/D-02: prepend order; breaks preserved (pre-wrap).
</success_criteria>

<output>
Create `.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-04-SUMMARY.md` when done.
</output>
