---
phase: 12-image-ocr-pipeline-right-click-side-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - package.json
  - test/vision-google.mjs
  - test/image-resolve.mjs
  - test/panel-render.mjs
  - src/panel-mock.ts
autonomous: true
requirements: [VIS-01, IMG-02, IMG-03, IMG-05]
must_haves:
  truths:
    - "Every downstream module can import VisionProvider, ImageResult, ImageState, and TranslateImageMessage/Response from types.ts without redefining them"
    - "tsc typechecks chrome.sidePanel.open() (the @types/chrome bump landed)"
    - "A display-name-to-ISO-639 lookup resolves the configured target language to a Translation v2 'target' code"
    - "Wave 0 test files exist and run (RED is acceptable until their subjects ship)"
  artifacts:
    - path: "src/types.ts"
      provides: "VisionProvider, ImageResult, ImageState, ImageEntry, 'translateImage' MessageType, TranslateImageMessage, TranslateImageResponse, Settings.googleApiKey, languageToIso()"
      contains: "interface VisionProvider"
    - path: "package.json"
      provides: "@types/chrome bumped to a version exposing sidePanel.open()"
      contains: "@types/chrome"
    - path: "test/vision-google.mjs"
      provides: "VIS-01 request/response test scaffold"
    - path: "test/image-resolve.mjs"
      provides: "VIS-03 + result-state-derivation test scaffold"
    - path: "test/panel-render.mjs"
      provides: "IMG-02/03/05 render-state test scaffold"
    - path: "src/panel-mock.ts"
      provides: "Vision/Translation fixture exports for the new tests"
  key_links:
    - from: "src/providers/vision-google.ts (plan 02)"
      to: "src/types.ts"
      via: "import type { VisionProvider, ImageResult }"
      pattern: "VisionProvider"
    - from: "src/background.ts (plan 05)"
      to: "chrome.sidePanel.open"
      via: "@types/chrome bump"
      pattern: "sidePanel\\.open"
---

<objective>
Lay the type + build foundation every other Phase 12 plan depends on: the `VisionProvider` interface and `ImageResult`/`ImageState`/message/Settings types, a display-name→ISO language map, the `@types/chrome` bump that lets `tsc` typecheck `chrome.sidePanel.open()`, and the three Wave 0 test scaffolds plus their mock fixtures.

Purpose: This is the interface-first contract task. Every Wave 2+ module (provider, pure utils, renderer, worker, panel) imports these types instead of inventing them, and the build gate (`tsc`) must accept `sidePanel.open()` before Plan 05 uses it. Per the Nyquist rule, the test files must exist now (RED until their subjects ship) so no subject ships untested.

Output: Edited `src/types.ts`, bumped `package.json`, three new `test/*.mjs` scaffolds, and `src/panel-mock.ts` fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-CONTEXT.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-RESEARCH.md
@.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces these new symbols (later plans consume them):

- `VisionProvider` (interface, types.ts) — `{ name: string; ocrTranslate(imageBase64: string, mime: string, targetLang: string, apiKey: string): Promise<ImageResult> }`
- `ImageResult` (interface, types.ts) — `{ originalText: string; translatedText: string; detectedLang: string; confidence: number; usage?: { inputTokens: number; outputTokens: number } }`
- `ImageEntry` (type, types.ts) — one panel session-list entry; a discriminated union over per-entry states: `{ kind: 'loading'; id; thumbnailUrl? } | { kind: 'populated'; id; thumbnailUrl?; result: ImageResult; lowConfidence: boolean } | { kind: 'no-text'; id; thumbnailUrl? } | { kind: 'error'; id; thumbnailUrl?; errorKind: ErrorKind; message: string }`
- `ImageState` (type, types.ts) — the panel-level render input: `{ kind: 'empty' } | { kind: 'list'; entries: ImageEntry[] }`
- `'translateImage'` — added to the `MessageType` union (types.ts:57-72)
- `TranslateImageMessage` (interface, types.ts) — `{ type: 'translateImage'; payload: { srcUrl: string; tabId: number; dedupKey: string } }`
- `TranslateImageResponse` (interface, types.ts) — success `{ result: ImageResult; noText?: false } | { noText: true }`; failure `{ error: string; kind: ErrorKind }`
- `Settings.googleApiKey: string` (field) + `DEFAULT_SETTINGS.googleApiKey: ''`
- `languageToIso(displayName: string): string` (function, types.ts) — maps a `SUPPORTED_LANGUAGES` display name to its ISO-639-1 code (A3)
- `src/panel-mock.ts` exports: `VISION_POPULATED`, `VISION_EMPTY`, `VISION_LOW_CONFIDENCE`, `TRANSLATE_V2_SAMPLE`, `XSS_PROBE`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bump @types/chrome for sidePanel.open()</name>
  <files>package.json</files>
  <read_first>
    - package.json (devDependencies: @types/chrome is "^0.0.246")
    - .planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-RESEARCH.md §Standard Stack + §Pitfall 7 (0.0.246 has sidePanel but NOT open(); need ≥0.0.258; latest 0.2.0)
  </read_first>
  <action>
    Bump the `@types/chrome` devDependency from `^0.0.246` to `^0.0.258` (or later) by running `npm install -D @types/chrome@^0.0.258`. This is a dev-only type bump — no runtime dependency change. After install, confirm `node_modules/@types/chrome/index.d.ts` declares `open` in the `sidePanel` namespace (0.0.246 only has getOptions/getPanelBehavior/setOptions/setPanelBehavior). If `^0.0.258` still lacks `open()`, bump further (latest observed is 0.2.0) until `open` is present, then pin to that version. Do NOT add any runtime dependency; the zero-runtime-dep law holds.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('node_modules/@types/chrome/index.d.ts','utf8'); if(!/\bopen\s*\(/.test(t.split('namespace sidePanel')[1]?.split('namespace ')[1]?'':t)){} ; const ns=t.indexOf('namespace sidePanel'); const seg=t.slice(ns, ns+4000); process.exit(/\bopen\s*[:(]/.test(seg)?0:1)"</automated>
  </verify>
  <done>package.json @types/chrome is ≥0.0.258 and the installed index.d.ts sidePanel namespace exposes open().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 12 type contracts + languageToIso map to types.ts</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts (full — TranslationProvider:44-48, SearchResult:92-105, SearchTranslatedMessage:107-114, SearchTranslatedResponse:122-129, MessageType union:57-72, Settings:18-26, SUPPORTED_LANGUAGES:166-186, DEFAULT_SETTINGS:189-203, migrateSettings:206-213)
    - .planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-PATTERNS.md §"src/types.ts (EDIT)" (each new symbol's analog line)
    - .planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-RESEARCH.md §Code Examples (VisionProvider/ImageResult shape) + Assumption A3 (name↔ISO map)
  </read_first>
  <behavior>
    - languageToIso('English') returns 'en'; languageToIso('Japanese') returns 'ja'; languageToIso('Chinese (Simplified)') returns 'zh-CN' (or 'zh'); every entry in SUPPORTED_LANGUAGES resolves to a non-empty ISO code.
    - languageToIso for an unknown/free-text display name falls back to a sensible default (return the input lowercased trimmed, or 'en') rather than throwing.
    - migrateSettings({}) yields googleApiKey === '' (DEFAULT_SETTINGS spread provides it; no migration code needed).
    - The discriminated unions ImageEntry and ImageState compile and each variant is reachable (a switch over .kind with a `never` default typechecks).
  </behavior>
  <action>
    Add to `src/types.ts`, mirroring the cited analogs:
    1. `VisionProvider` interface as a sibling of `TranslationProvider` (44-48): `name: string` and `ocrTranslate(imageBase64: string, mime: string, targetLang: string, apiKey: string): Promise<ImageResult>`.
    2. `ImageResult` interface modeled on `SearchResult` + the RESEARCH Code Examples shape: `originalText`, `translatedText`, `detectedLang` (ISO code or display name for "Detected: X → Y"), `confidence` (mean word confidence 0..1, for D-04), optional `usage: { inputTokens: number; outputTokens: number }`.
    3. `ImageEntry` discriminated union (per-entry IMG-05 / D-04 states): `loading`, `populated` (carries `result: ImageResult` and a `lowConfidence: boolean` flag — D-04 low-confidence is a populated entry with an amber badge, NOT its own kind beyond the flag), `no-text`, `error` (carries `errorKind: ErrorKind` and `message: string`). Each variant carries an `id: string` (the dedupKey) and optional `thumbnailUrl?: string`.
    4. `ImageState` panel-level union: `{ kind: 'empty' }` and `{ kind: 'list'; entries: ImageEntry[] }`.
    5. Add `'translateImage'` to the `MessageType` union (57-72).
    6. `TranslateImageMessage extends Message` cloning `SearchTranslatedMessage` (107-114): payload `{ srcUrl: string; tabId: number; dedupKey: string }`.
    7. `TranslateImageResponse` cloning `SearchTranslatedResponse` (122-129) contract: success `{ result: ImageResult; noText?: false }`, no-text `{ noText: true }`, failure `{ error: string; kind: import('./errors.js').ErrorKind }`.
    8. Add `googleApiKey: string` to `Settings` (after `braveApiKey:25`, same top-level rationale: read in worker, never in a message) and `googleApiKey: ''` to `DEFAULT_SETTINGS` (after 202). No migrateSettings change — the spread covers it.
    9. `languageToIso(displayName: string): string` — a small lookup over `SUPPORTED_LANGUAGES` display names → ISO-639-1 codes (English→en, Japanese→ja, Korean→ko, Chinese (Simplified)→zh-CN, Chinese (Traditional)→zh-TW, Spanish→es, French→fr, German→de, Italian→it, Portuguese→pt, Dutch→nl, Russian→ru, Polish→pl, Turkish→tr, Arabic→ar, Hindi→hi, Vietnamese→vi, Thai→th, Indonesian→id), with a graceful fallback for unknown values. Used by the provider (Plan 02) for Translation v2 `target` and by the panel "Detected: X → Y" line. No fenced code in this file beyond the implementation itself.
  </action>
  <verify>
    <automated>npm run build && node -e "import('./dist/types.js').then(m=>{const a=require('assert'); a.equal(m.languageToIso('English'),'en'); a.equal(m.languageToIso('Japanese'),'ja'); a.equal(m.migrateSettings({}).googleApiKey,''); console.log('ok')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>tsc compiles; languageToIso resolves English→en and Japanese→ja; DEFAULT_SETTINGS/migrateSettings provide googleApiKey:''; VisionProvider/ImageResult/ImageEntry/ImageState/TranslateImage* are exported and consumable.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scaffolds + panel-mock fixtures</name>
  <files>test/vision-google.mjs, test/image-resolve.mjs, test/panel-render.mjs, src/panel-mock.ts</files>
  <read_first>
    - test/serp.mjs (L1-60 — import from dist/, parseHTML from linkedom, node:test + node:assert/strict, fresh-mount-per-test helper, MOCKS/XSS_PROBE import from dist/search-mock.js)
    - src/search-mock.ts (the fixture-export shape to mirror in panel-mock.ts)
    - .planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-RESEARCH.md §Validation Architecture (Wave 0 Gaps list + Phase Requirements→Test Map) + §Code Examples (Vision/Translation v2 response shapes for fixtures)
  </read_first>
  <action>
    Create `src/panel-mock.ts` mirroring `src/search-mock.ts`: export `VISION_POPULATED` (a Vision `images:annotate` response with `responses[0].fullTextAnnotation.text` containing line breaks and `pages[0].property.detectedLanguages[0].languageCode='ja'` plus word-level `confidence` values averaging ≥0.6), `VISION_EMPTY` (`responses[0]` with NO fullTextAnnotation — the no-text case), `VISION_LOW_CONFIDENCE` (fullTextAnnotation present but word confidences averaging <0.6), `TRANSLATE_V2_SAMPLE` (`{ data: { translations: [{ translatedText, detectedSourceLanguage: 'ja' }] } }`), and `XSS_PROBE` (an OCR/translation string carrying `<img src=x onerror=...>` to prove textContent-only rendering).

    Create the three `test/*.mjs` scaffolds in the `test/serp.mjs` style (import subjects from `dist/`, NEVER the SW console — MEMORY law; node:test + node:assert/strict; linkedom parseHTML where DOM is needed). Each file declares the tests its subject must pass (the subject lands in Waves 2-4). It is acceptable for these to be RED now (subject not yet built) — the Nyquist rule requires the test file to EXIST, not yet pass:
    - `test/vision-google.mjs` (VIS-01): mock global `fetch`; assert `GoogleVisionProvider.ocrTranslate` POSTs to `VISION_ENDPOINT` with `?key=` and a DOCUMENT_TEXT_DETECTION body, then to `TRANSLATE_V2_ENDPOINT` with `q`/`target`/`format:'text'`; assert it maps the two responses → `ImageResult`; assert VISION_EMPTY yields the no-text path (NO second call); assert the key never appears in a logged/thrown string.
    - `test/image-resolve.mjs` (VIS-03 + state derivation): assert MIME guard accepts supported types and flags unsupported→re-encode; assert downscale long-edge math caps at the configured long edge and keeps within Vision limits; assert result-state derivation: empty→no-text, mean confidence <0.60→low-confidence, error→error.
    - `test/panel-render.mjs` (IMG-02/03/05): assert renderPanel renders populated (thumbnail + "Detected: X → Y" + translation + original), no-text, low-confidence (amber badge), and error states; assert D-01 prepend order (newest first); assert D-02 breaks preserved (the text class carries the pre-wrap class); assert XSS_PROBE is rendered as text (no element injected).
  </action>
  <verify>
    <automated>npm run build && node --test test/vision-google.mjs test/image-resolve.mjs test/panel-render.mjs 2>&1 | grep -Eq 'tests [0-9]+' && node -e "import('./dist/panel-mock.js').then(m=>{const a=require('assert');['VISION_POPULATED','VISION_EMPTY','VISION_LOW_CONFIDENCE','TRANSLATE_V2_SAMPLE','XSS_PROBE'].forEach(k=>a.ok(m[k]!==undefined,k));console.log('ok')})"</automated>
  </verify>
  <done>The three test files exist, are discovered by `node --test`, and import from dist/; panel-mock.ts exports all five fixtures. RED on subject-missing assertions is acceptable at this wave; the files must run and the mock fixtures must load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings storage → worker | googleApiKey added to Settings; it must only ever be read in the worker (this plan defines the field; Plans 02/05 enforce the read-site) |
| test fixtures → renderer | panel-mock OCR/translation strings (incl. XSS_PROBE) flow into panel-render in Plan 04; this plan seeds the adversarial fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information disclosure | `Settings.googleApiKey` type field | mitigate | Field is top-level (braveApiKey precedent), documented "read in worker only, never in a message"; enforcement lives in Plans 02/05. No serialization of the key into messages is introduced here. |
| T-12-02 | Tampering (XSS) | panel-mock `XSS_PROBE` fixture | mitigate | Seed an adversarial OCR/translation string now so Plan 04's renderer test proves textContent-only rendering. |
| T-12-SC | Tampering | npm install (@types/chrome bump) | mitigate | Dev-only version bump of an existing first-party DefinitelyTyped package; RESEARCH Package Legitimacy Audit records no new packages and no [ASSUMED]/[SUS]/[SLOP] verdicts → no blocking-human gate required. |
</threat_model>

<verification>
- `npm run build` (tsc gate) passes with the new types and the @types/chrome bump.
- `node --test test/{vision-google,image-resolve,panel-render}.mjs` discovers and runs all three files.
- `languageToIso` and `googleApiKey` defaulting verified by the inline node checks.
</verification>

<success_criteria>
- All Phase 12 type contracts are exported from types.ts and compile.
- @types/chrome exposes sidePanel.open() to tsc.
- languageToIso maps every SUPPORTED_LANGUAGES entry to an ISO code with a graceful fallback.
- Three Wave 0 test files + panel-mock fixtures exist and load.
</success_criteria>

<output>
Create `.planning/phases/12-image-ocr-pipeline-right-click-side-panel/12-01-SUMMARY.md` when done.
</output>
