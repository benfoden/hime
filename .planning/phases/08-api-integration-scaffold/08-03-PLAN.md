---
phase: 08-api-integration-scaffold
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/background.ts
  - test/unit.mjs
autonomous: true
requirements: [SRCH-05, SRCH-06, XLT-01]
must_haves:
  truths:
    - "Sending a searchTranslated message reaches the background worker, which reads the Brave key from getSettings() (storage), never from the message payload, and returns { results } or { error, kind }"
    - "When sourceLanguage === targetLanguage the handler returns direct: true on the response"
    - "Two rapid searchTranslated messages for the same normalized query issue only ONE Brave fetch (in-flight dedup)"
    - "An empty stored braveApiKey returns { error, kind: 'auth' } without attempting a fetch"
    - "A testBraveKey message runs a minimal Brave query using the stored key and returns { ok: true } or { ok: false, error, kind }"
  artifacts:
    - path: "src/background.ts"
      provides: "case 'searchTranslated' + case 'testBraveKey' in onMessage, module-scope in-flight dedup Map, BraveSearchClient wiring"
      contains: "case 'searchTranslated'"
    - path: "test/unit.mjs"
      provides: "dedup, source==target, key-from-storage routing, and 429-surfacing assertions for the handler logic"
      contains: "dedup"
  key_links:
    - from: "src/background.ts"
      to: "src/brave-search.ts"
      via: "BraveSearchClient.search() inside case 'searchTranslated'"
      pattern: "braveClient\\.search|BraveSearchClient"
    - from: "src/background.ts"
      to: "getSettings"
      via: "settings.braveApiKey read in the worker (XLT-01)"
      pattern: "settings\\.braveApiKey"
---

<objective>
Wire the Brave round-trip into the background message dispatcher. Add `case 'searchTranslated'` and
`case 'testBraveKey'` to the existing onMessage switch, a module-scope in-flight dedup Map keyed on the
normalized query (D-05), and the source==target short-circuit flag (D-06). The Brave key is read from
`getSettings()` in the worker only — never from the message payload — satisfying XLT-01.

Purpose: This is the contract the whole feature is built on. Phase 11's search page and the Plan-04
options test button both send these messages; the dedup + short-circuit + error surfacing all live
here so no UI has to re-implement them.

Output: Extended `src/background.ts`, new handler-logic test cases in `test/unit.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-api-integration-scaffold/08-CONTEXT.md
@.planning/phases/08-api-integration-scaffold/08-RESEARCH.md
@.planning/phases/08-api-integration-scaffold/08-01-SUMMARY.md
@.planning/phases/08-api-integration-scaffold/08-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols/behaviors (Phase 11 + Plan 04 consume the message contract):
- `case 'searchTranslated'` in background.ts onMessage switch — { results, direct? } | { error, kind }
- `case 'testBraveKey'` in background.ts onMessage switch — { ok } | { ok: false, error, kind }
- module-scope `inFlightSearches: Map<string, Promise<SearchResult[]>>` dedup (D-05)
- module-scope `braveClient = new BraveSearchClient()` instance
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add searchTranslated + testBraveKey handlers with in-flight dedup and direct flag</name>
  <read_first>
    - src/background.ts (the file being modified — getSettings(), the onMessage switch dispatch, the 'translate'/'predict' cases showing the { error, kind } convention, the async IIFE + `return true` channel-open pattern)
    - src/brave-search.ts (BraveSearchClient.search signature from Plan 02 — the transport this handler calls)
    - src/types.ts (SearchTranslatedMessage / SearchTranslatedResponse / TestBraveKeyMessage shapes from Plan 01)
    - .planning/phases/08-api-integration-scaffold/08-RESEARCH.md "Pattern 2 In-Flight Dedup Map", "background.ts case sketch", Pitfall 4
    - test/unit.mjs lines 603-624 (precedent: background.ts logic tested inline as pure logic, no chrome import)
  </read_first>
  <files>src/background.ts, test/unit.mjs</files>
  <behavior>
    - dedup: a Map keyed by normalized query (trim().toLowerCase()); when a key is already present, the second caller awaits the SAME promise rather than starting a new fetch → only one underlying search call for two same-query submits
    - dedup cleanup: the Map entry is deleted in BOTH success and failure paths (try/finally) so a failed query does not leave a hanging entry (Pitfall 4)
    - source==target: when sourceLanguage === targetLanguage, the response carries direct: true; otherwise direct is absent/false
    - empty key: when the stored braveApiKey is '' the handler responds { error, kind: 'auth' } and does NOT call search()
    - 429 surfacing: when search() rejects with kind 'search_quota', the response is { error: <message containing "quota">, kind: 'search_quota' } with no retry
  </behavior>
  <action>
    In src/background.ts: import `BraveSearchClient` from './brave-search.js' and the
    `SearchTranslatedMessage` / `SearchResult` types from './types.js'. At module scope add
    `const braveClient = new BraveSearchClient();` and `const inFlightSearches = new Map<string,
    Promise<SearchResult[]>>();`. Add `case 'searchTranslated'` to the onMessage switch following the
    existing case structure (cast `message as SearchTranslatedMessage`, read { query, sourceLanguage,
    targetLanguage } from payload). Call `const settings = await getSettings();` and read
    `settings.braveApiKey` — the key comes from storage ONLY, never from msg.payload (XLT-01). If the
    key is falsy → `sendResponse({ error: 'Brave API key not configured — add it in options', kind:
    'auth' })` and break (no fetch). Compute `const isDirect = sourceLanguage === targetLanguage;`
    (D-06). Compute `const dedupKey = query.trim().toLowerCase();` (D-05). If
    `inFlightSearches.has(dedupKey)`, await the existing promise, then sendResponse({ results, direct:
    isDirect }) (wrap in try/catch to surface the same { error, kind } on a shared rejection) and break.
    Otherwise create `const promise = braveClient.search(query, settings.braveApiKey, { count: 10 });`,
    `inFlightSearches.set(dedupKey, promise);`, then try/finally: on success delete the entry and
    sendResponse({ results, direct: isDirect }); on error delete the entry and sendResponse({ error:
    err.message, kind: err.kind ?? 'unknown' }). Use try/finally so the Map entry is ALWAYS cleaned up
    (Pitfall 4). Do NOT add any retry logic for 429 (D-07). Add `case 'testBraveKey'`: read settings,
    if braveApiKey falsy → sendResponse({ ok: false, error: 'Brave API key is empty — enter it in
    options', kind: 'auth' }); else try `await braveClient.search('test', settings.braveApiKey, { count:
    1 })` → sendResponse({ ok: true }); catch → sendResponse({ ok: false, error: err.message, kind:
    err.kind ?? 'unknown' }). Per D-04 the test uses count:1 to minimize quota cost. Do NOT log the
    Brave key. Keep the existing `return true` at the end of the listener (async channel).
    In test/unit.mjs: add a handler-logic test block. Since background.ts is not importable under
    node:test (it calls chrome.* at module load), test the EXTRACTABLE pure logic the same way the
    existing predict tests do (test/unit.mjs lines 603-624 precedent): (a) dedupKey normalization
    'Hello '.trim().toLowerCase() === 'hello'; (b) a small dedup harness — a Map<string,Promise> where
    two calls for the same key reuse one underlying counter-incrementing async fn, asserting the counter
    increments once; (c) isDirect = (source === target) boolean logic for equal vs unequal pairs; (d)
    that an empty-key guard `!apiKey` short-circuits before a fetch-call flag is set. Keep these as
    logic-only assertions mirroring the inline-algorithm precedent — do not import chrome.
  </action>
  <verify>
    <automated>cd /home/ben/code/hime && npm test 2>&1 | grep -iE "dedup|direct flag|empty.key|searchTranslated" </automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes
    - `grep -c "case 'searchTranslated'" src/background.ts` == 1
    - `grep -c "case 'testBraveKey'" src/background.ts` == 1
    - `grep -c "inFlightSearches" src/background.ts` ≥ 3 (declare + has/get + set + delete)
    - `grep -c "settings.braveApiKey" src/background.ts` ≥ 1 (key read from storage, XLT-01)
    - `grep -v '^[[:space:]]*//' src/background.ts | grep -c "msg.payload.braveApiKey\|payload.apiKey\|payload.key"` == 0 (key NEVER read from payload)
    - `grep -c "finally" src/background.ts` ≥ 1 within the searchTranslated case region (dedup cleanup, Pitfall 4)
    - A dedup test asserts two same-key calls increment an underlying counter exactly once
    - A test asserts isDirect logic returns true for equal source/target and false for unequal
  </acceptance_criteria>
  <done>background.ts dispatches searchTranslated (with dedup, direct flag, empty-key guard, error surfacing) and testBraveKey (count:1), reads the key only from getSettings(), never retries 429, and the dedup/direct/normalization logic tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension page → background onMessage | searchTranslated/testBraveKey arrive here; the handler must read the key from storage, not the payload |
| handler → BraveSearchClient | passes the storage-sourced key to the transport; the page never sees it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information disclosure | searchTranslated handler | mitigate | Key read via `getSettings()` (chrome.storage.local) inside the worker; never read from msg.payload — grep gate asserts no payload key read. Satisfies XLT-01. |
| T-08-08 | Denial of resource | rapid duplicate submits | mitigate | In-flight dedup Map (D-05) collapses same-query submits to one Brave fetch; testBraveKey uses count:1 (D-04); no 429 retry (D-07) |
| T-08-09 | Denial of resource (self-DoS) | dedup Map leak | mitigate | try/finally deletes the Map entry on success AND failure (Pitfall 4) so a failed query never leaves a hanging entry |
| T-08-SC | Tampering | npm installs | n/a | No package installs in this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `cd /home/ben/code/hime && npm test` — full suite green (tsc compile + node:test)
- `tsc --noEmit` succeeds (background.ts imports BraveSearchClient and the new types cleanly)
- Manual (deferred to phase-end, see 08-VALIDATION.md): load unpacked dist, send a searchTranslated from a page console with a live key, confirm a result array returns and the key is not exposed to the page
</verification>

<success_criteria>
- searchTranslated returns { results, direct? } on success and { error, kind } on failure, reading the key only from storage (XLT-01)
- sourceLanguage === targetLanguage yields direct: true (SRCH-05 / D-06)
- duplicate in-flight queries collapse to one Brave fetch (SRCH-06 / D-05), with try/finally cleanup
- empty stored key short-circuits to { error, kind: 'auth' } without a fetch
- testBraveKey runs a count:1 validation query and returns { ok } | { ok: false, error, kind }
- no 429 auto-retry; no key logging
</success_criteria>

<output>
Create `.planning/phases/08-api-integration-scaffold/08-03-SUMMARY.md` when done.
</output>
