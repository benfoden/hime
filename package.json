{
  "name": "hime",
  "version": "1.0.0",
  "description": "AI-native IME Chrome extension",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "mkdir -p dist/icons && cp src/icons/* dist/icons/ 2>/dev/null || true && cp src/*.html dist/ 2>/dev/null || true && cp src/*.css dist/ 2>/dev/null || true && cp manifest.json dist/",
    "watch": "tsc --watch",
    "dev": "npm run build && npm run watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "typescript": "^5.2.2"
  },
  "keywords": ["chrome-extension", "ime", "translation", "ai"],
  "license": "MIT"
}
